# Shipping_Design — 配送判定フロー・配送マスター設計

> **This document follows FOUNDATION.md.**
> **If any conflict exists, FOUNDATION.md takes precedence.**

ステータス: **ドラフト v1.0 — 設計レビュー待ち(実装は承認後)**
作成日: 2026-09-07
位置づけ: **v1.0 スコープの第3段**(`Architecture.md` Definition of Done 条件8)
階層: ARCHITECTURE層(`Master_Design.md` と同階層)

```
商品マスター  →  発送テンプレート  →  【配送マスター】  →  ラベル自動作成
 (Master_Design)   (Master_Design §4.5)     (本書)          (実装フェーズ)
```

---

## 0. この文書の役割 — なぜテンプレートより先に書くのか

**判定ロジックが先、テンプレートは後である。**

発送テンプレートが持つべき区分は、**判定フローが何を区別するかによって決まる**。
逆順で作ると、**判定が使わない区分のテンプレートを作り、必要な区分を取りこぼす。**

> **テンプレートは判定結果を表現する器であって、器から判定を決めるのではない。**

本書で判定フローを確定し、**そこから逆算して `ST0001`〜 の必要数を確定する**(§5)。

---

## 1. 現在の運用(確定事実のみ)

**推測を書かない。以下は `Business_Catalog.md` BIZ-SAL-06 で確認済みの事実である。**

### 1.1 判断の原則

> **送料を最優先に決定している。** 判断要素は **配送地域 / 契約送料 / サイズ** の3つのみ。
> 配送スピード・配送品質は**現在の判断基準に含まれない**(2026-09-06 確認)。

### 1.2 現行の配送ルール(楽天)

| 配送先 | 配送会社・配送方法 |
|--------|-------------------|
| **通常**(下記以外) | **ヤマト運輸 宅急便コンパクト** |
| **北海道 / 九州 / 中国地方 / 四国** | **佐川急便 60〜100サイズ** |

**このルールは送料比較の"結果"であって、原則ではない**(2026-09-05 Decision Log)。

### 1.3 ラベル作成の経路 — 2系統に分かれる

| モール | 経路 |
|--------|------|
| **楽天** | 楽天注文 → 配送会社を決定 → **ヤマト運輸 / 佐川急便の各専用システム** → 印刷 |
| **Amazon** | 注文取得 → **プライスター** → Amazon専用ラベル → 印刷 |

### 1.4 未確認事項(本書で埋めないもの)

| # | 未確認 | 影響 |
|---|--------|------|
| 1 | **Amazon側の配送会社・配送方法の決まり方** | Amazon分の判定フローを書けない |
| 2 | **契約送料の実額**(会社 × サイズ × 地域) | 送料テーブルを作れない(§4で扱う) |
| 3 | **同梱ルール** | **判定フローの前提が変わる**(§3.2 — 最重要) |
| 4 | **配送不可地域・例外**(沖縄・離島など) | 地域区分を確定できない |
| 5 | ネコポス等の小型便の使用有無 | テンプレート数に影響 |

---

## 2. 判定フロー(v1.0 — 現在の運用の再現)

**原則: AIはまず現在の運用を正確に再現する**(`Architecture.md` 機能ロードマップ)。

```
受注
  │
  ├─① 商品を特定 → 発送テンプレートID → 発送サイズ区分
  │     └─ 未整備(テンプレートID or 商品重量が空) ──────→ 🙋 人へ回す
  │
  ├─② 同梱判定  ⚠️ ルール未確認
  │     └─ 1注文に複数商品 ─────────────────────────→ 🙋 人へ回す
  │
  ├─③ 配送先の都道府県 → 地域区分
  │     └─ 区分に該当しない(離島・海外等) ────────────→ 🙋 人へ回す
  │
  ├─④ (発送サイズ区分 × 地域区分) → 配送判定ルール
  │     └─ 該当ルールなし ────────────────────────────→ 🙋 人へ回す
  │        ↓
  │     配送会社・配送方法が確定
  │
  ├─⑤ 制約チェック(重量上限・寸法上限)
  │     └─ 超過 ──────────────────────────────────────→ 🙋 人へ回す
  │
  └─⑥ ラベル作成経路へ引き渡す
        楽天   → ヤマト / 佐川の各システム
        Amazon → プライスター  ⚠️ 判定ルールが未確認
```

### 2.1 設計の中心 — 各段に「人へ回す」出口を置く

**判定できない注文を、AIが推測で処理しないための構造である。**

`Master_Design.md` §3.5 の安全規則(未整備商品のラベルをAIが作ってはならない)を、
**フローの形として実装する。** 出口が無い段があれば、そこでAIは推測を強いられる。

> 🚫 **「たぶん60サイズ」で処理するAIを作らない。**
> 送料の過少申告・差額請求・誤配送は、**推測した瞬間に発生する。**

### 2.2 自動化率は「人へ回す」割合で決まる

**この6段のうち、どれか1つでも人へ回れば、その注文は自動化されない。**
したがって v1.0 稼働後の最重要指標は次の1つである。

```
自動化率 = ラベルが自動作成できた注文 ÷ 全注文
```

**現時点でこの値を押し下げる最大の要因は ② 同梱である**(§3.2)。

---

## 3. 判定フローが露わにした設計上の穴

**フローを書いたことで、テンプレート設計だけでは見えなかった問題が2つ出た。**

### 3.1 ⚠️ Amazon側の判定ルールが存在しない

楽天の判定ルールは確定しているが、**Amazon側は未確認**である。
プライスターが配送会社を決めているのか、人が選んでいるのかも分かっていない。

**したがって v1.0 の判定フローは、現時点では楽天分しか書けない。**

| 選択肢 | 評価 |
|--------|------|
| **A. 楽天を先行させ、Amazonは判定ルール確認後に追加** | ✅ **推奨。** 楽天は判定ルールが確定しており、今すぐ設計できる |
| B. Amazon側の確認を待ってから両方を同時に設計 | ❌ 確定している側まで止まる |

> `Business_Catalog.md` §10 OQ 9(★最優先の着手順)は、**この判断そのものである。**
> **判定ルールが確定している楽天から着手するのが最短**である。

### 3.2 🚨 同梱ルールが未確認 — 判定フローの前提を壊す

**これが v1.0 最大の未解決事項である。**

発送テンプレートは**商品1点に紐付く**。しかし **1注文に複数商品が含まれると、
発送サイズは個々のテンプレートでは決まらない。**

```
商品A(60サイズ) + 商品B(60サイズ) を同梱
        ↓
実際の発送サイズは 60 ではない(80 かもしれないし、2個口かもしれない)
        ↓
どちらのテンプレートを使っても正しくない
```

**v1.0 の方針(提案):**

| 注文の形 | 扱い |
|---------|------|
| **1注文1商品(数量1)** | ✅ 自動判定する |
| 1注文1商品(数量2以上) | 🙋 人へ回す(まとめると size が変わるため) |
| 1注文に複数商品 | 🙋 人へ回す |

> ⚠️ **これは自動化率の上限を規定する。**
> **「1注文1商品・数量1」の割合が、そのまま v1.0 の自動化率の天井になる。**
> この割合は**注文データから今すぐ測定できる**。測ってから着手順を決めるべきである(§6 OQ 3)。
>
> 仮にこの割合が低ければ、**同梱ルールの確認が最優先事項へ繰り上がる。**

---

## 4. v1.0 の配送マスター構成

### 4.1 結論 — 送料テーブルは v1.0 では作らない

**現行ルールをそのまま持つ。送料からの最安選択は v1.1 以降とする。**

| 選択肢 | 評価 |
|--------|------|
| **A. 現行の判定ルールを持つ**(サイズ × 地域 → 会社・方法) | ✅ **v1.0 で採用** |
| B. 送料テーブルを作り、最安を計算で選ぶ | ❌ v1.0 では採らない(下記) |

**Bを採らない理由は2つある。**

1. **契約送料の実額が未確認である。** データが無いものは設計できても実装できない
2. **より重要 — 計算結果が現行運用と食い違う危険がある。**
   契約送料の入力が1つでも不正確なら、AIは**現在と違う配送会社を選ぶ**。
   これは **「AIはまず現在の運用を正確に再現する」** という原則に正面から反する

> **現行ルールは4行程度で表現できる。** 送料テーブルは(会社 × 方法 × サイズ × 地域)の
> 4次元で数百行になる。**少ない行で確実に再現できるものを、多い行で不確実に再現しない。**

**送料テーブルは v1.1 で追加し、まず「現行ルールの検証」に使う。**
ルールと計算結果が食い違えば、それは**ルールが古いか送料が変わったかの発見**になる
(`Master_Design.md` §3.9 のデータ品質保証と同じ考え方)。

### 4.2 マスターの構成(2つだけ)

#### ① 地域区分マスター

| 列 | 内容 |
|----|------|
| 都道府県 | 47件 |
| **地域区分** | `通常` / `北海道` / `九州` / `中国` / `四国` など |
| 状態 | `有効` / `使用停止` |

> ⚠️ **区分の定義が未確定。** 「中国地方」に山口県を含むか、**沖縄・離島をどう扱うか**が未確認(§6 OQ 2)。
> 現行ルールの表に沖縄が現れないため、**別扱いされている可能性が高い。**

#### ② 配送判定ルールマスター

| 列 | 内容 |
|----|------|
| **判定ルールID** | `DR0001`。主キー |
| **モール** | `楽天` / `Amazon`(v1.0 は楽天のみ) |
| **発送サイズ区分** | 発送テンプレートから来る値 |
| **地域区分** | 地域区分マスターから来る値 |
| **配送会社** | ヤマト運輸 / 佐川急便 |
| **配送方法** | 宅急便コンパクト / 飛脚宅配便 など |
| 適用順 | 複数ルールが該当したときの優先度 |
| 状態 | `有効` / `使用停止` |
| 登録日 / 使用停止日 / 備考 | 追跡用 |

**現行ルールをこの表で表すと、こうなる**(発送サイズ区分は §5 で確定):

| モール | 発送サイズ区分 | 地域区分 | 配送会社 | 配送方法 |
|--------|--------------|---------|---------|---------|
| 楽天 | 宅急便コンパクト | 通常 | ヤマト運輸 | 宅急便コンパクト |
| 楽天 | 60 | 北海道・九州・中国・四国 | 佐川急便 | 飛脚宅配便 60 |
| 楽天 | 80 | 北海道・九州・中国・四国 | 佐川急便 | 飛脚宅配便 80 |
| 楽天 | 100 | 北海道・九州・中国・四国 | 佐川急便 | 飛脚宅配便 100 |

> ⚠️ **この表には穴がある。** 「60・80・100 で **通常地域**」の行が無い。
> 現行ルールは「通常はコンパクト」としか言っておらず、
> **コンパクトに入らない商品を通常地域へ送るとき何を使うかが未確認**である(§6 OQ 1)。
> **フローを書いたことで初めて見えた穴である。**

### 4.3 ライフサイクル — 追記専用(発送テンプレートと同じ)

**削除しない。役目を終えたら使用停止。ルールの内容は変更しない。**

理由は `Master_Design.md` §4.5 と同一である。
**判定ルールを後から書き換えると、過去の配送判断を再現できなくなる。**

```
10月「DR0002 で佐川80サイズと判定」と記録
12月 DR0002 の配送会社をヤマトへ変更
      ↓
10月の記録が「ヤマトで送った」ことになる ← 事実と違う
```

ルールを変えるときは**新しいルールを追加し、旧ルールを使用停止にする。**

---

## 5. 発送テンプレートへの要求(判定フローからの逆算)

**本書を書いた目的がここに出る。**

**判定フローが区別するのは、次の発送サイズ区分だけである。**

| 発送サイズ区分 | 判定で使われるか | テンプレート |
|--------------|----------------|------------|
| 宅急便コンパクト | ✅ 通常地域の既定 | **`ST0001` 必要** |
| 60 | ✅ 佐川ルールに登場 | **`ST0002` 必要** |
| 80 | ✅ 同上 | **`ST0003` 必要** |
| 100 | ✅ 同上 | **`ST0004` 必要** |
| ネコポス | ❌ **現行ルールに登場しない** | ⏸ **不要**(使用実態が確認されるまで作らない) |
| 120 以上 | ❌ 同上 | ⏸ **不要** |

> ✅ **判定フローを先に書いた効果が、ここで確認できた。**
> `ST0005`(ネコポス)・`ST0006`(100超)は**判定に現れないため、v1.0 では作らない。**
> 逆順で進めていれば、**使われないテンプレートを2つ作り、その実測にも時間を使っていた。**

**したがって発送テンプレートは `ST0001`〜`ST0004` の4種で確定する。**
必要な実測値は **4種 × 4値(縦・横・高さ・資材重量)= 16個** である。

---

## 6. Open Questions

| # | 事項 | なぜ必要か |
|---|------|-----------|
| **1** | 🎯 **コンパクトに入らない商品を「通常地域」へ送るとき何を使うか**(§4.2) | **現行ルールの穴。** 判定ルール表が埋まらない |
| **2** | **地域区分の正確な定義**(§4.2) | 中国地方に山口を含むか。**沖縄・離島の扱い**(現行ルールに現れない) |
| **3** | 🎯 **「1注文1商品・数量1」の割合**(§3.2) | **v1.0 の自動化率の天井を決める。** 注文データから今すぐ測定できる |
| **4** | 同梱ルール(§3.2) | 3の測定結果しだいで最優先へ繰り上がる |
| **5** | **Amazon側の判定ルール**(§3.1) | Amazon分の設計に必要。**楽天先行なら v1.1 で可** |
| **6** | 配送不可地域・例外 | 地域区分マスターの完成に必要 |
| **7** | 重量・寸法の上限チェックを v1.0 で行うか(§2 ⑤) | 現行運用で上限超過が起きているかによる |

---

## 7. 次のステップ

| 順 | やること | 前提 |
|----|---------|------|
| 1 | 本書のレビュー・承認 | — |
| 2 | **OQ 1・2 を確定**(判定ルール表の穴埋め・地域区分) | ヒアリング |
| 3 | **OQ 3 を測定**(1注文1商品の割合) | 注文データ |
| 4 | **`ST0001`〜`ST0004` の実測値を確定**(16個の数字) | §5 で4種に確定済み |
| 5 | 実装(発送テンプレート → 配送マスター → ラベル) | 上記すべて |

> **本書により、発送テンプレートは4種で確定した。** 実測すべき数値は16個である。
