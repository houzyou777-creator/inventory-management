# Master_Design — 商品マスター設計

> **This document follows FOUNDATION.md.**
> **If any conflict exists, FOUNDATION.md takes precedence.**

ステータス: **v2.0 — 設計確定・商品マスターへの列追加 実装完了(2026-09-07)**
作成日: 2026-09-06 / 更新日: 2026-09-07
位置づけ: **Phase4「Excel→DB移行」の設計基盤**
階層: ARCHITECTURE層(`Business_Catalog.md` / `Decision_Catalog.md` / `Process_Catalog.md` と同階層)
対象: 商品マスター(Single Source of Truth)および、それに紐付く全マスターの関係

---

## 0. この文書の役割

**MomijiStore OS における「商品とは何か」を確定する。**

商品マスターは、配送・広告・在庫・FBA・仕入・販売・AI判断のすべてが参照する中心である。
**ここが揺れると、その上に載るすべてが揺れる。** だからこの1点だけは、実装より先に設計を固める。

### 本書の作成方針 — 現物を数えてから書く

**本書は推測で書いていない。** 以下は 2026-09-06 に現物のExcelを読み取り専用で調査した実測値である。

| 対象 | 件数 | 出典 |
|------|------|------|
| 商品マスター | **962件** | `01_InventoryManagement/SourceData/商品マスター_単品_v1.0.xlsx`「商品マスター」 |
| 出品テーブル | **1,181件** | 同ファイル「出品テーブル」 |
| 在庫管理テーブル | **554件** | `SourceData/在庫管理テーブル_v1.1.xlsm`「在庫管理テーブル」 |

### 前提となる制約(既存の決定)

| 制約 | 出典 |
|------|------|
| **列名・内部管理ID・マスター構造は変更しない** | `CLAUDE.md` GS移行方針(Apps Script移植時のデータ互換のため) |
| **原価の正は商品マスター。既存原価を自動上書きしない** | 2026-08-03 決定(`sync_cost_master.py`) |
| **xlsm は openpyxl で保存禁止** | ボタン描画(drawing1.xml)が消えるため |
| **正本は増やしてはならない** | BL-5 |

> ⚠️ **本書は「作り直す」設計ではない。** 現行構造は既に「商品 1 : N 出品」という正しい形になっている。
> 本書はそれを**追認し、欠けている部分だけを補う**。既存のIDと列名は維持する。

---

## 1. 商品マスターの責務

### 中心となる原則

> **商品マスターは「その商品が何であるか」を持つ。「その商品がどうなっているか」は持たない。**

この一文が責務の境界そのものである。迷ったらここへ戻る。

| 問い | 持つ場所 |
|------|---------|
| **何であるか**(識別・属性・原価・寸法) | **商品マスター** |
| どこで、いくらで売っているか | 出品テーブル |
| いくつあるか | 在庫管理テーブル |
| いくつ売れたか / いくら使ったか | 販売・広告の実績(トランザクション) |
| どう判断し、どうなったか | Intelligence Layer(`intelligence.decisions`) |

### 責務(持つもの)

| # | 責務 | 意味 |
|---|------|------|
| 1 | **同一性の保証** | 実世界の1商品に、ただ1つのIDを与える。**これが最上位の責務** |
| 2 | **不変・準不変属性の保持** | JAN・商品名・種別・入数・寸法・重量 |
| 3 | **原価の正本** | 標準原価はここが正(2026-08-03 決定) |
| 4 | **全マスターの参照先** | 在庫・出品・仕入・広告・販売・判断のすべてが内部管理IDで紐付く |
| **5** | **外部データの検証基準(Ground Truth)** | **Amazonカタログ・楽天情報・仕入先情報と突き合わせ、外部側の誤りを検知する基準となる**(§3.10) |

> **責務5は、他の4つと性質が違う。** 1〜4は「正しく参照される」という受け身の役割だが、
> **5は「外部が正しいかを判定する」という能動的な役割**である。
> **正本とは、参照されるものであると同時に、外部を裁く基準でもある。**

### 責務でないもの(持たないもの)

**持たないと決めることは、持つと決めることと同じだけ重要である。**

| 持たないもの | 理由 | 正しい置き場 |
|-------------|------|-------------|
| 在庫数 | 保管場所ごとに異なり、常に変わる | 在庫管理テーブル |
| 販売価格 | **チャネルごとに異なる**(同一商品が楽天とAmazonで別価格) | 出品テーブル |
| チャネル別の識別子(SKU・ASIN) | 1商品が複数の出品を持つため1対1にならない | 出品テーブル |
| 販売実績・広告実績 | 時系列のトランザクション。マスターに集計値を持つと必ず古くなる | 実績データ |
| 判断の履歴 | 追記専用の別構造(BL-7) | `intelligence.decisions` |

> **⚠️ 現行の「販売チャネル」列はこの原則に反している(§3.2 で扱う)。**

---

## 2. 主キー設計

### 2.1 結論 — 内部管理ID(P連番)を主キーとする

**根拠は「他のすべての候補が実データで失格したこと」である。**

| 候補 | 実測 | 判定 |
|------|------|------|
| **内部管理ID**(P000001形式) | **重複 0 / 962件** | ✅ **主キーとする** |
| JAN | **欠損 232件(24.1%)** / 重複 35種(75行) | ❌ 欠損と重複の両方がある |
| 楽天SKU(単独) | 重複 19種(52行) | ❌ 別商品で使い回される |
| 楽天(商品管理番号, SKU)ペア | 重複 16種 | △ 照合キーには使えるが一意ではない |
| ASIN | 重複 3種 | ❌ かつAmazon専用で楽天商品を識別できない |
| 商品名 | — | ❌ 変わる。一意性の保証がない |

**自然キーをすべて捨て、代理キー(サロゲートキー)を主キーにする。**
自然キーが1つでも健全なら代理キーは不要だが、**実データ上どれも健全でない**。これが判断のすべてである。

### 2.2 JANを主キーにできない決定的な実例

「JANは商品固有だから主キーにできるはず」という直感は、実データでは成立しない。

```
JAN 4533141407514 に3商品が登録されている
   P000555  きんだい 冷ひやネックタオル  50本入り
   P000556  きんだい 冷ひやネックタオル 100本入り
   P000557  きんだい 冷ひやネックタオル 200本入り
```

**入数の異なる商品に、メーカーが同一JANを振っている。** これは登録ミスではなく実世界の事実である。
JAN重複35種のうち **28種はセット商品で説明できるが、残り7種は上記のような実世界由来**であった。

さらに **232件(24%)はJANそのものが無い**(短縮形の商品番号・AST-/SET-コードなど、JANを抽出できない商品)。
**4件に1件が識別できないキーは、主キーになりえない。**

### 2.3 ID体系 — 既存を維持する

| 接頭辞 | 対象 | 例 | 単位 |
|--------|------|-----|------|
| **P** | 商品(Product) | `P000001` | 実世界の1商品 |
| **C** | 出品(Channel listing) | `C000001` | 1チャネルでの1つの売り方 |
| **S** | 在庫(Stock) | `S000001` | 1商品 × 1保管区分 |

**採番規則(確定させたい事項):**

1. **連番は再利用しない。** 削除しても欠番のまま残す
2. **一度振ったIDは変更しない。** 商品名やJANが変わってもIDは変わらない
3. 桁数は6桁固定(P000001〜P999999)。**上限に達する前に桁を増やす判断が必要**(現在962件、上限まで余裕あり)

> **なぜ再利用しないのか:** 過去の判断記録(`intelligence.decisions.subject_id`)や実績が
> そのIDを指している。IDを再利用すると、**過去の記録が別商品を指すようになる**。
> 追記専用(BL-7)と同じ思想である。

### 2.4 自然キーの位置づけ — 「照合キー」として別に定義する

主キーではないが、**外部データ(モールCSV)と突き合わせるために不可欠**。一意制約とは別物として扱う。

| チャネル | 照合キー | 実測 | 注意 |
|---------|---------|------|------|
| **楽天** | **(商品管理番号, SKU管理番号) のペア** | ペア重複16種(すべて同一商品) | 🚫 **SKU単独をキーにしてはならない** |
| **Amazon** | **AmazonSKU** | **重複 0 / 529件** | 出品を一意に識別できる唯一のAmazon側キー |
| Amazon(参考) | ASIN | 重複 3種 | 商品の識別には使えるが**一意ではない** |

> 🚫 **楽天SKU単独での照合を禁止する。**
> 過去、SKU単独をキーに在庫照合を行い、**同一SKUを共有する別商品間で在庫数が混ざる事故**が発生している
> (銀のスプーン 98個 → 10個)。ペア照合はその対策であり、**実データ上も正しく機能している**
> (ペア重複16種を全数追跡した結果、**別商品を指す危険な重複は0件**)。

### 2.5 🚨 未解決の問題 — 同一商品が複数のIDで登録されている

**調査で発見した、主キー設計に直結する実害。**

| ASIN | 内部管理ID | 商品名 |
|------|-----------|--------|
| `B0743GXHY6` | **P000826 / P000886 / P000947** | ビオレu 【大容量】 つめかえ用4回分 1.35リットル |
| `B0GSQJXS8J` | **P000818 / P000885** | クーリア シール ボンボンドロップシール パールベール |
| `B0D226Y7VQ` | **P000863 / P000927** | ロイヤルカナン FHN インドア ロングヘアー 猫用 |

同一ASIN=同一商品でありながら、**内部管理IDが分かれている**。
AmazonSKUが出品時期ごとに異なる(`..._20260715_...` / `..._20251107_...`)ため、
**再出品のたびに新規商品として登録された**と考えられる。

**これは §1 の最上位責務「実世界の1商品にただ1つのIDを与える」の違反である。**

影響:
- 原価・在庫・実績が2〜3箇所に分散する
- AIが「この商品の過去の判断」を引くとき、**一部しか見つからない**
- 仕入判断で「在庫過多」を検知できない(在庫が分散するため)

### 2.6 同一性の判定ルール(名寄せ)— 新規に必要

§2.5 を再発させないため、**新規登録時に「既存商品と同じか」を判定する**手順を定める。

**判定の順序**(上から順に評価し、最初に該当したもので確定する)

| # | 条件 | 結論 |
|---|------|------|
| 1 | AmazonSKU が完全一致 | **既存の出品**。新規登録しない |
| 2 | (楽天商品管理番号, SKU) が一致 | **既存の出品**。新規登録しない |
| 3 | ASIN が一致 | ⚠️ **同一商品の可能性が高い → 人へ提示** |
| 4 | JAN一致 かつ 商品種別一致 かつ **入数一致** | ⚠️ **同一商品の候補 → 人へ提示** |
| 5 | いずれも該当しない | 新規商品として登録 |

> ⚠️ **未確認(2026-09-07 自己点検):** 本節は **「現状どう重複登録を防いでいるか」を確認せずに設計した。**
> `Architecture.md` Change Management「**追加する前に、現状どう解決しているかを確認する**」に照らすと、
> **手順を1つ飛ばしている。** 実装前に次を確認する必要がある。
>
> - 現在、新商品登録時に既存商品との重複をどう防いでいるか
> - `sync_cost_master.py register` の重複検出で足りているのか
> - §2.5 の重複3件は、**その仕組みが無いから**生じたのか、**運用の漏れ**なのか
>
> **原因が分からないまま判定ルールを足しても、同じ重複は防げない。**

> 🚫 **自動統合は禁止する。** 3・4 は候補提示までとし、統合の可否は人が判断する(`DEC-MST-01`)。
> **理由は §2.2 の実例そのもの。** JAN 4533141407514 は入数違いに同一JANが振られており、
> **「JANが一致するから同一商品」と判断すると、50本入りと200本入りを統合してしまう。**
> だから条件4に「入数一致」を含め、それでもなお人が確認する。

---

## 3. 管理項目

### 3.1 現行10列の評価

| # | 列名 | 実測 | 判定 |
|---|------|------|------|
| 0 | 内部管理ID | 重複0 | ✅ **維持**(主キー) |
| 1 | JAN | 欠損232 / 重複35種 | ✅ **維持**(照合キー。一意制約は付けない) |
| 2 | 商品名 | — | ✅ 維持 |
| 3 | 商品種別 | 単品704 / セット258 | ⚠️ **維持するが値の点検が必要**(§3.3) |
| 4 | 標準原価 | **未設定 26件** | ✅ 維持(原価の正本) |
| 5 | 販売チャネル | 楽天448 / Amazon365 / 両方149 | 🚫 **廃止を提案**(§3.2) |
| 6 | 販売ステータス | **962件すべて「販売中」** | ⚠️ **維持するが未活用**(§3.4) |
| 7 | 登録日 | — | ✅ 維持 |
| 8 | 最終更新日 | — | ✅ 維持 |
| 9 | 備考 | — | ✅ 維持 |

### 3.2 🚫「販売チャネル」列の廃止を提案する

**この列は出品テーブルから完全に導出できる。** 商品マスターが持つ必要がない。

```
P000001 の販売チャネル = 出品テーブルで内部管理ID=P000001 の行の チャネル を集めたもの
```

**「両方」という値の存在が、二重管理である証拠である。** 出品テーブルには「両方」というチャネルは無く
(楽天652 / Amazon529 の2値のみ)、マスター側だけが独自の第3の値を持っている。
楽天出品を1つ取り下げた瞬間、**出品テーブルは正しくなるがマスターは「両方」のまま古くなる**。

これは **BL-5「正本は増やしてはならない」** に反する。同じ事実に正本が2つある。

| 選択肢 | 評価 |
|--------|------|
| **A. 列を廃止し、必要なときに出品テーブルから導出する** | ✅ **推奨**。正本が1つになる |
| B. 列を残し、出品テーブル更新時に自動再計算する | △ 同期処理を作れば動くが、**壊れる箇所を1つ増やす**(Always Simple に反する) |
| C. 現状維持 | ❌ 古い値が残り続ける |

> ⚠️ **ただし GS移行方針は「列名・マスター構造は変更しない」と定めている。**
> 列の削除は構造変更にあたるため、**この提案は承認を要する**。
> 承認されない場合は B ではなく **「列は残すが参照しない(非推奨列として明記)」** を提案する。
> 参照されなければ古くても害が出ないためであり、これは移行時の互換性も壊さない。

### 3.3 商品種別 — 値の点検が必要

セット258件が登録されている一方で、**「【まとめ買い】」と商品名にありながら種別が「単品」**の行が
JAN重複の調査中に複数見つかった(例: JAN `4994527860703` の P000330 / P000901)。

**種別は §2.6 の名寄せ条件4で使うため、値が正しくないと同一性判定を誤る。**
実装前に全962件の点検が必要(§6 OQ 2)。

### 3.4 販売ステータス — 定義はあるが一度も使われていない

**962件すべてが「販売中」。** 販売停止・廃番になった商品が1件も記録されていない。

しかし `Decision_Catalog.md` には既に対応する判断が登録されている:

| Decision ID | 判断 | 結果の書き戻し先 |
|---|---|---|
| `DEC-SAL-03` | 値下げではなく**販売停止・在庫処分**にするか | → 販売ステータス |
| `DEC-PUR-01` | その商品を扱うか | → 販売ステータス(取扱終了) |

**判断は存在するのに、結果を書き戻す先が使われていない。** これは Intelligence Layer 導入時と同じ構造
(判断はあるが記録先がない)。**この列を活かすことが、AIが「今売っている商品」を正しく把握する前提になる。**

推奨する値: `販売中` / `販売停止` / `廃番` / `取扱終了`
(⚠️ 値の確定は要ヒアリング — §6 OQ 3)

### 3.5 追加する項目 — ✅ 物理属性3種を先行追加(2026-09-06 承認)

**承認済み。** 重量・梱包サイズ・梱包資材の3種を先行して追加する。

> **これは配送マスター完成までの暫定運用ではない。**
> **商品そのものの物理属性**として恒久的に保持する(2026-09-06 ユーザー指示)。
> したがって将来 配送マスターができても、これらは商品マスターに残り続ける。

#### なぜ物理属性は商品マスターに属するのか

§1 の原則「商品マスターは**その商品が何であるか**を持つ」に照らせば、判定は明快である。

| | 商品そのものか | 置き場 |
|---|---|---|
| **重量・寸法・梱包資材** | ✅ **物として決まっている。チャネルでも配送会社でも変わらない** | **商品マスター** |
| 配送会社(ヤマト/佐川) | ❌ 地域・送料・カート状況で変わる**判断の結果** | 将来の配送マスター |
| 配送方法(宅急便コンパクト等) | ❌ 同上 | 将来の配送マスター |

> 🚫 **当初 §3.5 で提案していた「配送区分(宅急便コンパクト可否など)」は取り下げる。**
> これは商品の物理属性ではなく**配送方法の判断**であり、商品マスターに置くと責務が滲む。
> 「この商品は宅急便コンパクトに入るか」は、**重量・寸法と配送方法の規格を突き合わせて導出できる**。
> 導出できるものをマスターに持たない(§3.8・BL-5)。

#### 追加する列 — ✅ 確定(2026-09-06)

現行10列の**末尾に追加する**(理由は §3.8)。**この2列がすべてである。**

**✅ 2026-09-07 実装完了。** 本番ファイルへ反映済み(バックアップ取得・全検証 PASS)。

| # | 列名 | 型 | 必須 | 内容 |
|---|------|-----|------|------|
| 11 (K) | **発送サイズ区分** | 文字列 | ○ | `ネコポス` / `宅急便コンパクト` / `60サイズ` / `80サイズ` / `100サイズ` の**5値のみ**。**マスターは作らない**(§4.5) |
| 12 (L) | **商品重量(g)** | 数値 | ○ | **商品そのものの重量。** 梱包を含まない。**送料計算とデータ品質保証の両方に使う**(§3.10) |

**追加はこの2列だけである。**

#### ✅ 入力規則(2026-09-07 実装済み)

**入力ミスがそのままAI判定の誤りになるため、Excel側で弾く。**

| 列 | 規則 | 範囲 |
|----|------|------|
| **K 発送サイズ区分** | **5値のみ選択可**(プルダウン)。空欄可 | `K2:K5000` |
| **L 商品重量(g)** | **0以上の数値のみ**。空欄可 | `L2:L5000` |

**選択肢は `ネコポス` / `宅急便コンパクト` / `60サイズ` / `80サイズ` / `100サイズ` の5つ。**

> ✅ **`60` ではなく `60サイズ` とした理由:**
> `60` のような数値だけの文字列は、**Excelが数値として保存してしまう**。
> すると セルの値=`60`(数値) と 判定ルール側=`"60"`(文字列) が一致せず、**AI判定が壊れる。**
> **`60サイズ` は数値化されないため、この不具合が構造的に起こらない。**
>
> **この表記は 商品マスター・配送判定ルール・AI判定 のすべてで統一する。**

**空欄を許すのは、962件を段階的に埋めるためである**(§6.1)。
規則は「入力するなら正しい形式で」という制約であり、未入力を妨げない。


**当初は5列を想定していたが、2列まで削れた。**

| 段階 | 追加列 |
|------|-------|
| 2026-09-06 当初案 | 重量・縦・横・高さ・梱包資材 の**5列** |
| テンプレート方式 | テンプレートID・商品重量・寸法上書き3列 の**5列** |
| 完全継承(上書き廃止) | テンプレートID・商品重量 の**2列** |
| **2026-09-07 確定** | **発送サイズ区分・商品重量 の2列**(テンプレートマスターも廃止) |

> ✅ **商品マスターへの追加は、この2列だけである。**

#### 発送重量は保持しない — 資材重量も管理しない(2026-09-07 確定)

**商品マスターが持つのは「商品そのものの重量」だけである。**

```
❌ 当初案: 発送重量 = 商品重量 + 資材重量(テンプレート)
✅ 確定  : 商品重量のみを保持する。資材重量は管理しない
```

**理由は §4.5 と同じ — 梱包資材が固定されていないためである。**
専用箱・リサイクルダンボール・袋・メーカー箱を都度使い分けるため、
**資材重量を保持しても正確な判定にはならない。**

| 効果 | 内容 |
|------|------|
| **商品マスターが配送に依存しない** | 持つのは「物としての重さ」だけ。どの箱で送るかを一切知らない |
| 正本が1つ | 同じ事実を2箇所に持たない(BL-5) |
| **実測が不要** | 資材を測る作業そのものが消えた |

> ⚠️ **ネコポスの重量上限(1kg)は、商品重量を基準に判定する。**
> **資材の重さは判定に入らないため、境界付近では誤差が出る。**
> **これは許容する** — v1.0 は100%自動判定を目的とせず、
> **境界付近はAIが候補提示し、人が最終確認する**(`Shipping_Design.md` §6.1)。

> ⚠️ **「梱包サイズ」であって「商品サイズ」ではない**(寸法はテンプレート側)。
> 対して **重量は「商品そのもの」であって発送重量ではない**(商品マスター側)。
> **寸法は箱の事実、重量は物の事実。** この非対称が、両者を別の場所に置く理由そのものである。

#### 梱包資材と配送会社の境界

`宅急便コンパクト専用BOX` のように、**資材そのものが配送会社に紐付く場合がある。**
ここで責務が滲まないよう、境界を明示する。

| 持つ場所 | 内容 |
|---------|------|
| **発送テンプレートマスター** | 「その型の**発送サイズは何で、何cm・何gか**」 |
| **商品マスター** | 「この商品は**どの発送テンプレートか**」 |
| 将来の配送マスター | 「その**発送サイズ**をこの地域へ送るなら、どの会社のどの方法が安いか」 |

**商品マスターも発送テンプレートも、送料・地域・配送会社は持たない。**
この分け方であれば、配送会社が変わっても商品マスターは書き換えなくてよい。

#### 整備レベル — 3段階で進める(2026-09-07 確定)

**商品重量は全商品で管理対象とする。** ラベル判定のためだけでなく、
**Amazonカタログやメーカー情報を監査するための基準になる**ため(§3.10)。

**ただし、重量が揃うまで自動化を止める必要はない。**

| レベル | 状態 | 何ができるようになるか |
|--------|------|---------------------|
| **レベル1** | 発送サイズ区分のみ入力 | ✅ **ラベル自動化を開始できる** |
| **レベル2** | + 商品重量(候補を人が承認) | ✅ **監査(DQA)を開始できる** |
| **レベル3** | 商品重量が**実測済み・確認済み** | ✅ **完全整備** |

> ✅ **レベル1でラベル自動化が動き出す。**
> 判定フローで商品重量を使うのはネコポスの上限確認だけであり(`Shipping_Design.md` §2 ⑤)、
> **実測が必要な255件が全体のブロッカーにならない。**

#### 🚫 未入力時の扱い(安全規則)

**配送情報が未整備の商品について、AIは配送ラベルを自動作成してはならない。**

推測で寸法を決めると、**送料の過少申告・追加請求・誤配送**につながる。
未整備の商品は候補から外し、**人へ回す**。

**判定条件(レベル1を満たすか):**

```
ラベル自動作成が可能 = 発送サイズ区分 が設定されている
                     かつ (区分 ≠ ネコポス、または 商品重量 が設定されている)
```

**ネコポスだけ重量を要するのは、上限(1kg)の確認が必要なため**である。

#### AIが重量候補を提示するときの形式(2026-09-07 確定)

**AIは値だけを出さない。次の3点を必ず添える。**

| 項目 | 内容 | 例 |
|------|------|-----|
| **候補重量** | 提案する値 | `480g` |
| **根拠** | どこから導いたか | `Amazonカタログ` / `メーカー情報` / `商品名(14g×30本から推定)` |
| **確信度** | どれくらい確からしいか | `高` / `中` / `低` |

**人はこの3点を見て承認する。** 根拠と確信度が無ければ、人は値の妥当性を判断できない。

> 🚫 **AIが提示した値をそのまま正本へ登録してはならない。**
> **AIが候補を提示 → 人が承認 → 商品マスター更新** の順を必ず通す。

### 3.6 🚫「重量の出所」列は追加しない — 提案は取り下げ(2026-09-07)

**一度は M列「重量の出所」の追加を提案したが、取り下げる。**

#### 取り下げの理由

| # | 理由 |
|---|------|
| 1 | **商品マスターは最も長く運用するマスターであり、列は本当に必要なものだけにする** |
| 2 | **出所は判断履歴で追える。** 商品マスターは現在の正本、Decision Log はそこへ至る理由 |
| 3 | **情報源は今後増える。** そのたびに列や値を増やすより、Decision Log 側で扱うほうが柔軟 |

#### ⚠️ 私の提案は、本書 §1 の責務の線を越えていた

**§1 で「商品マスターは**その商品が何であるか**を持つ。**どうなっているか**は持たない」と定めた。**

| | 分類 |
|---|---|
| 「この商品の重量は 480g である」 | ✅ **属性** — 商品マスター |
| 「その 480g は Amazonカタログから取った」 | ❌ **その値がどう決まったかの経緯** — Decision Log |

**出所は履歴である。** 私は「1回の参照で済む」という性能上の理由で属性側へ寄せようとしたが、
**監査は定期実行であって注文ごとではない。** 性能を理由に責務を曲げる必要はなかった。

#### 代わりに — 記録の形式で監査を成立させる

**列を足さずに「どの重量がAmazon由来か」を引けるようにする。**
`intelligence.decisions` の `reason` を次の書式で統一する。

```
根拠: <出所> / 確信度: <高|中|低> / <補足>
```

**例:**
```
根拠: Amazonカタログ / 確信度: 中 / 商品名「14g×30本」から内容量420g、包装込みで妥当
根拠: 実測 / 確信度: 高 / 2026-09-10 計量
```

**この書式なら、既存のMCPツールだけで監査に必要な問いに答えられる。**

| 問い | 引き方 |
|------|--------|
| どの重量がAmazon由来か | `search_decisions(decision_type="DEC-MST-05", keyword="根拠: Amazonカタログ")` |
| 実測済みはどれか | 同上 `keyword="根拠: 実測"` |
| この商品の重量はどう決まったか | `search_decisions(subject_id="P000123")` |

> ✅ **新しい列も、新しいテーブルも要らない。** 書式を決めるだけで済んだ。
> **`Architecture.md` Change Management「追加する前に、現状どう解決しているかを確認する」の実践である**
> — 既にある仕組み(Intelligence Layer)で解けるなら、構造は足さない。

#### 整備レベルの判定(列を追加しない版)

| レベル | 判定方法 |
|--------|---------|
| レベル1 | 商品マスター: 発送サイズ区分あり |
| レベル2 | 商品マスター: + 商品重量あり |
| **レベル3** | **Decision Log に `根拠: 実測` の記録がある** |

**レベル3だけは商品マスターを見ても分からない。** これは設計上の欠陥ではなく、
**「実測したという事実」が履歴だからである。**

### 3.7 追加を提案する項目(未承認)### 3.7 追加を提案する項目(未承認)

**すべて「これが無いと特定の業務がAIに委譲できない」という具体的な理由がある。**
理由を示せない項目は提案しない。

| 追加項目 | これが無いと何ができないか | 関連 |
|---------|------------------------|------|
| **入数 / セット数** | セット258件の数量が**商品名の中にしかない**。原価計算・名寄せ(§2.6 条件4)・在庫換算ができない | `DEC-MST-01` |
| **メーカー / ブランド** | 仕入リサーチの集約単位。名寄せの補助 | `DEC-PUR-01` |
| **廃盤フラグ / 後継品の内部管理ID** | `BIZ-PUR-02`「廃盤・後継品なし」例外の判定が**どこにも記録できない**。全力仕入れの根拠になる重要項目 | `DEC-PUR-01` |

### 3.8 列は末尾に追加する(技術的制約)

**既存の列位置を動かしてはならない。** 現行コードが**列番号で**商品マスターを読んでいるためである。

| 参照元 | 参照方法 | 影響 |
|--------|---------|------|
| `mcp-server/src/server.py` | 0起点の索引 `0,1,2,4,5,6` + 見出し名の照合 | 途中挿入で**全件が誤った列を読む** |
| `02_Analytics/Python/sync_cost_master.py` | `PM_ID..PM_NOTE = range(1, 11)` (1〜10列) | 同上。原価の書き込み先がずれる |
| 稼働中VBA(`Module_Inventory` / `Module_InventoryOps`) | **商品マスターを参照していない** | 影響なし |

**末尾(11列目以降)への追加であれば、上記すべてに影響しない。**
`server.py` は見出し名も検証しているため、途中挿入時は黙って壊れるのではなく明確に失敗する
(`_verify_headers`)。それでも**壊してから気づくより、壊さない位置に足す**。

> ✅ **旧VBAは凍結済み(2026-09-07)。** `在庫管理システム_v1.0_VBAコード.bas` は現行マスターと不整合だった。
> 仕入先=L(12)・ステータス=O(15)・登録日=Q(17)・更新日=R(18) という**18列構成**を前提としているが、
> 実際の商品マスターは**10列**で、仕入先列は存在しない。
>
> **⚠️ 2026-09-06 の記述「追加列(K〜O)を破壊する」は不正確だったため訂正する。**
> 書き込み先は O(15)/Q(17)/R(18) であり、**K・L を直接破壊はしない**。実際の危険は次の2つだった。
>
> | # | 危険 | 内容 |
> |---|------|------|
> | 1 | **モジュール名の衝突** | `VB_Name = "Module_Inventory"` を宣言しており、**稼働中の `Module_Inventory.bas` と完全に同名**。インポートすると現行モジュールを置き換える恐れがある |
> | 2 | **L列の誤読** | 371行 `VLookup(..., PM_SUPPLIER=12=L列)` が、**L列へ追加予定の「商品重量(g)」を仕入先名として読み取る** |
>
> **対処:** 警告ヘッダーを付与し `.bas.frozen_20260907_列不整合` へ改名してインポート不可にした。
> **削除はしない**(過去の設計意図が読めなくなるため)。

### 3.9 持たせない項目(意図的な除外)

| 除外する項目 | 理由 |
|-------------|------|
| 販売価格 | チャネルごとに異なる → 出品テーブル |
| 在庫数 | 保管区分ごとに異なる → 在庫管理テーブル |
| **配送会社 / 配送方法** | **判断の結果であって商品の属性ではない**(2026-09-06 確定)→ 将来の配送マスター |
| **宅配サイズ区分(60/80/100)** | 梱包寸法から**導出できる**。かつ配送会社ごとに規格が異なる → 持たない |
| 最終仕入日 / 最終販売日 | 実績から導出できる。持つと必ず古くなる |
| 累計販売数 / 累計利益 | 同上。集計はトランザクションから行う |
| 商品ごとの最低利益率 | **現時点で商品別の例外運用が確認できていない**(BL基準は一律15%)。実在を確認できてから追加する |

---

### 3.10 データ品質保証(Data Quality Assurance)— 商品マスターは外部を裁く基準である

**重量・寸法は送料計算のためだけの項目ではない。外部データの誤りを検知する監査項目である。**
(2026-09-07 訂正 — 当初は送料計算のためだけの項目として記録していた)

#### なぜ監査が必要か — 相乗りカタログの構造的な問題

**Amazonは他社が作成したカタログへ相乗り出品する。** そのカタログの重量・サイズは
**自社が入力したものではなく、正しさを保証できない**。誤登録は実際に存在する。

```
Amazonカタログの寸法が誤っている
        ↓
その寸法で送料を見積もる
        ↓
実際の荷物はもっと大きい / 重い
        ↓
配送会社から差額請求 / 想定より送料が高い
        ↓
利益が計算と合わない(しかも原因が分からない)
```

**原因が分からないまま利益がずれ続けるのが、この問題の最も厄介なところである。**
送料の実績だけを見ても「なぜ高いのか」は分からない。**基準と突き合わせて初めて分かる。**

#### 検知の仕組み

| 比較元 | 比較対象 | 検知したいこと |
|--------|---------|--------------|
| **Amazonカタログ** | 重量・寸法 | **相乗りカタログの誤登録**(最重要) |
| **楽天の登録情報** | 重量・寸法 | 自社登録のミス |
| **仕入先情報** | 重量・寸法・入数 | 仕様変更・認識違い |

#### 🔒 比較元の抽象化 — 設計を取得方法に依存させない(2026-09-07 確定)

**監査が必要とするのは「外部の値」であって「取得手段」ではない。**
API・CSV・レポート・手入力のいずれで得た値でも、監査の設計は変わらない。

**比較元が満たすべき条件(契約)はこれだけである。**

| # | 条件 | 理由 |
|---|------|------|
| 1 | **商品を特定できる識別子を持つ**(内部管理ID へ辿れること) | どの商品の値か分からなければ比較できない |
| 2 | **比較対象の値を持つ**(重量・寸法など) | — |
| 3 | **いつ時点の値かが分かる**(取得日時) | 外部データは変わる。**いつの値と比較したか不明だと、差分の意味が変わる** |

**この3つを満たせば、取得手段は何でもよい。**

```
Amazon API      ─┐
CSVダウンロード  ─┤
レポート出力     ─┼→ (商品識別子, 値, 取得日時) →  監査ロジック  → 差分の提示
仕入先の資料     ─┤                                    ↑
手入力          ─┘                          ここは取得手段を知らない
```

> **なぜ分けるのか:** **取得手段は変わる。** モールのAPI仕様は改定され、
> レポート形式も変わり、提供が終了することすらある。
> **変わるものに設計を縛ると、取得手段が変わるたびに設計をやり直すことになる。**
> 契約だけを固定しておけば、**取得手段の変更は実装の差し替えで済む。**

**実利:** 手入力も正当な比較元である。
**取得の自動化を待たなくても、監査は今日から始められる。**
気になる商品を数十件だけ手で入れて突き合わせても、**相乗りカタログの誤登録は検知できる。**

> ⚠️ **取得方法の決定は実装工程へ送る**(§6 OQ 21 は保留)。
> **設計側で確定させるのは「何を、何と、いつ時点で比べ、差分をどう扱うか」までである。**

**AIは差分を検出し、警告する。**

```
⚠️ P000123 の重量がAmazonカタログと 480g 乖離しています
   商品マスター: 1,200g  /  Amazonカタログ: 720g
   → 送料が誤っている可能性があります。Amazonカタログを確認してください
```

#### 🚫 外部から自動上書きしてはならない — 独立性が監査の前提

**商品マスターの重量・寸法を外部データで自動更新すると、監査そのものが成立しなくなる。**

```
Amazonカタログの誤った値でマスターを上書きする
        ↓
マスターとカタログが一致する
        ↓
差分が消える = 誤りを永久に検知できなくなる
```

**基準が対象に合わせて動いたら、それはもう基準ではない。**

これは **標準原価とまったく同じ扱い**である
(2026-08-03 決定「マスターの既存原価は**絶対に自動上書きしない**。不一致は一覧提示してユーザー判断」)。
**正本に対する規律として一貫している。**

| 操作 | 可否 |
|------|------|
| 外部データとの差分を**検出して提示する** | ✅ AIが行う |
| 差分を**自動でマスターへ反映する** | 🚫 **禁止** |
| どちらが正しいかを**判断する** | 人(`DEC-MST-04`) |

#### この役割が「重量は不要かもしれない」という検討を否定する

2026-09-07 の検討で、**「現在の配送会社判定はサイズのみなので、重量は不要かもしれない」**
(旧 OQ 18)と記録したが、**これは誤りだった。**

> **送料判定に使っていないからこそ、外部データを検証する独立した基準として価値がある。**
> 判定に使う値だけを持つ設計は、**間違いに気づく手段を持たない設計**でもある。

**商品重量は必須項目とする。** 962件の計測は避けられない。

#### 実測値であることの重要性(§6 OQ 10 への影響)

**監査基準は、監査対象と同じ出所であってはならない。**
メーカー公称値をマスターへ入れると、**Amazonカタログも同じ公称値を参照している場合、
両者が一致してしまい誤りを検知できない**。
**独立性を保つには実測が望ましい** — この観点は OQ 10 の判断材料に加える。

## 4. 他マスターとの関係

### 4.1 全体像

```
                  ┌──────────────────────────┐
                  │   商品マスター (P)         │  ← Single Source of Truth
                  │   「何であるか」            │  + 発送サイズ区分 / 商品重量
                  └────────────┬─────────────┘
                               │ 内部管理ID
        ┌──────────────┬───────┴──────┬───────────────┐
        │              │              │               │
   ┌────┴────┐   ┌─────┴─────┐  ┌─────┴────┐   ┌──────┴──────┐
   │ 出品(C)  │   │  在庫(S)   │  │ 仕入・発注│   │ 判断記録     │
   │ N件      │   │  N件       │  │ N件      │   │ intelligence │
   │ チャネル別│   │ 保管区分別 │  │ 未実装   │   │ .decisions   │
   └────┬────┘   └───────────┘  └─────────┘   └─────────────┘
        │ 出品ID
   ┌────┴──────────────┐
   │ 広告実績 / 販売実績 │  ← ★ 出品単位であって商品単位ではない
   └───────────────────┘

   ※ 発送サイズ区分は商品マスターの列。専用マスターは持たない(§4.5)
```

### 4.2 ★ 最重要 — 何を「商品」で繋ぎ、何を「出品」で繋ぐか

**本章の核心。粒度を取り違えると、数字が静かに壊れる。**

| 紐付く対象 | 繋ぐキー | 理由 |
|-----------|---------|------|
| 在庫 | **内部管理ID(P)** | 物理的な在庫は1つ。楽天用とAmazon用に分かれてはいない |
| 配送属性(サイズ・重量) | **内部管理ID(P)** | 物のサイズはチャネルで変わらない |
| 仕入・発注 | **内部管理ID(P)** | 仕入れるのは商品であって出品ではない |
| **広告実績** | **出品ID(C)** | 楽天RPPはSKU単位、Amazon SPはASIN/SKU単位。**チャネルをまたいで合算できない** |
| **販売実績** | **出品ID(C)** | 同一商品でもチャネルごとに価格・手数料が異なる |
| 販売価格 | **出品ID(C)** | チャネル別 |

> ⚠️ **典型的な失敗:** 広告実績を商品単位で持つと、楽天RPPとAmazon SPが合算され、
> **どちらの広告が効いたのか永久に分からなくなる**。
> `BIZ-ADS-03` の指標(利益額 → TACOS → ROAS)はモール別に評価する必要があるため、
> **広告は必ず出品IDで繋ぐ。**
>
> 逆に **在庫を出品単位で持つと、同一商品の在庫が二重計上される。**
> 実際に楽天の `-a` 商品(Amazon共有在庫)は、楽天側の在庫数が「見せ在庫」であり
> **正はAmazon側**という運用になっている。在庫を商品単位に置くことでこの問題が構造的に解消する。

### 4.3 在庫管理テーブルとの関係

**現状(実測):**

| 項目 | 実測 | 評価 |
|------|------|------|
| 在庫行数 | 554件 | 商品マスター962件の **57.6%** |
| 内部管理IDの重複 | 0 | ✅ 現在は 商品 1 : 在庫 1 |
| マスターに無いIDの参照 | **0件** | ✅ 参照整合性は保たれている |
| **在庫行が存在しない商品** | **408件** | ⚠️ §6 OQ 4 |
| 保管区分 | **554件すべて「自社倉庫」** | ⚠️ **FBA在庫が入っていない** |

`保管区分マスター` には `OWN 自社倉庫` / `FBA` が定義済みで、FBAには「**将来対応**」と記載されている。

> ⚠️ **FBA在庫が在庫管理テーブルに存在しない。**
> `BIZ-INV-06`(FBA納品の判断)と `DEC-INV-03`(FBAへ納品するか)は登録済みだが、
> **その結果としてのFBA在庫を受け取る場所が空のまま**である。
> AIがFBA納品を提案するには、FBA在庫を含めた全体像が要る。

**設計上の帰結:** 在庫は **商品 1 : N 在庫(保管区分ごと)** とする。
現在は N=1(自社倉庫のみ)だが、FBA対応時に自然に N=2 へ増える。**構造を変えずに拡張できる。**

### 4.4 Intelligence Layer との関係

昨日実装した `intelligence.decisions` は `subject_type` / `subject_id` で対象を指す。
**ここに規約を定める必要がある**(現在は自由文字列)。

| subject_type | subject_id に入れる値 | 使う判断の例 |
|-------------|---------------------|------------|
| `product` | **内部管理ID**(`P000021`) | `DEC-PUR-01` 仕入可否 / `DEC-INV-03` FBA納品 / `DEC-SAL-05` 配送 |
| `listing` | **出品ID**(`C000021`) | `DEC-SAL-02` 価格改定 / `DEC-ADS-01` 広告 |
| `supplier` | 仕入先名 | `DEC-PUR-03` 発注 |
| `system` | 対象システム名 | `DEC-SYS-*` |

> **なぜ規約が要るのか:** `search_decisions(subject_id="P000021")` で過去の判断を引くとき、
> **記録側がASINや商品名を入れていたら、何も見つからない。**
> 「新しい施策を提案する前に、まずここを引く」というツールの前提が崩れる。
>
> **価格・広告の判断は出品ID、仕入・在庫・配送の判断は内部管理ID。** §4.2 と同じ境界である。

### 4.5 発送サイズ区分 — マスターは作らない(2026-09-07 確定)

**商品マスターの列として直接持つ。専用マスターは作らない。**

```
ネコポス / 宅急便コンパクト / 60サイズ / 80サイズ / 100サイズ   ← この5値のみ
```

#### なぜマスターを作らないのか

| 理由 | 内容 |
|------|------|
| 1 | **5値しかなく、固定値で十分管理できる** |
| 2 | **1列のコード表のためにマスターを追加すると、管理対象だけが増える** |
| 3 | **現状、発送サイズ区分を独立したマスターとして管理していない**(Change Management の原則) |

> **`Architecture.md` Change Management「追加する前に、現状どう解決しているかを確認する」に従った結論である。**
> **v1.0 は最小構成で現状業務を正確に再現することを優先する。**

#### 設計の経緯 — 削ることで固まった

**この結論に至るまでに、設計は4回削られた。** 経緯自体が判断材料になるため残す。

| 段階 | 形 | 削れた理由 |
|------|-----|----------|
| 2026-09-06 ① | **梱包テンプレート(資材が軸)10種** | — |
| 2026-09-06 ② | 完全継承・上書き廃止 → **商品マスターは2列** | 「一部だけ上書き」は全商品に確認コストを課す |
| 2026-09-07 ③ | **発送テンプレート(サイズが軸)6種 → 5種** | **判定に使う軸で設計する。** 判定フローを先に書き、判定が区別する区分だけを残した |
| 2026-09-07 ④ | **寸法・資材重量を廃止** | **梱包資材が固定されていない。** 固定でないものを固定値で持てば実態と合わない |
| 2026-09-07 ⑤ | **マスターそのものを廃止** ✅ | 残ったのは5値のコード表だけだった |

> **設計は足すことではなく、削ることで固まった。**
> 当初の「10種のテンプレート × 5列の商品マスター × 実測20個」は、
> **最終的に「商品マスターの2列」だけになった。**

#### 判定フローとの接続

**発送サイズ区分は、配送判定ルールマスターの入力になる**(`Shipping_Design.md` §5.2)。

```
商品マスター(発送サイズ区分) × 地域区分 → 配送判定ルール → 配送会社・発送方法
```

⚠️ **発送サイズ区分は「使える発送方法の下限」であって、使う発送方法そのものではない**
(`Shipping_Design.md` §3.3)。関東でコンパクトに収まる荷物を60サイズで送ることがある。

#### 🚫 梱包資材はOSの管理対象にしない

**現場は 専用ダンボール・リサイクルダンボール・袋・メーカー箱 を状況に応じて使い分けている。**
**どの箱を使うかは現場判断とし、OSでは管理しない。**

> **手段(資材)を管理せず、結果(サイズ区分)だけを管理する。**
> 管理しても使われない情報は、管理コストだけが残る。

### 4.6 将来のマスター(現時点では作らない)

| 候補 | 現状 | 判断 |
|------|------|------|
| 仕入先マスター | 発注は6経路・スプレッドシート管理 | `BIZ-PUR-03` の一元化時に検討 |
| 保管区分マスター | **既に存在**(OWN / FBA) | 維持 |
| **配送マスター**(配送会社・配送方法・資材の対応) | 未整理 | **作ることが確定**(2026-09-06)。`Business_Catalog.md` §10 OQ 10 の確認後に設計する。**商品マスターの物理属性(重量・寸法・梱包資材)を入力として受け取り、配送会社と配送方法を決める**のがこのマスターの責務 |
| ブランド/メーカーマスター | 未整理 | §3.5 の項目が単なる文字列で足りるうちは作らない(Always Simple) |

> **迷ったら作らない。** マスターを増やすほど、引き継ぎ時に説明すべきことが増える。

---

## 5. 移行方針(Phase4 への接続)

**本書は設計であって、移行手順書ではない。** ここでは原則だけを定める。

| 原則 | 内容 |
|------|------|
| 1 | **列名・内部管理IDは変えない。** DBのテーブルも既存列名との対応表を維持する(`CLAUDE.md` GS移行方針) |
| 2 | **移行中にDBを正本としない。** Phase4完了まで、DB上の業務データは参照専用(`momiji-stack.yml` の注記どおり) |
| 3 | **§2.5 の重複3件は移行前に解消する。** 壊れたまま移すと、DB側で一意制約を掛けられない |
| 4 | **Intelligence Layer は例外。** Excelに対応物を持たないため、既にDBが正本(2026-09-05 Decision Log) |

---

## 6. Open Questions(設計レビューで確定させたい事項)

| # | 事項 | 必要な判断 |
|---|------|-----------|
| **1** | **「販売チャネル」列の廃止(§3.2)** | 廃止 / 非推奨として残す / 現状維持。**GS移行方針(構造を変更しない)との兼ね合いで承認が必要** |
| **2** | **商品種別の点検(§3.3)** | 「【まとめ買い】」なのに単品と登録された行がある。全962件の点検を行うか |
| **3** | **販売ステータスの値(§3.4)** | `販売中 / 販売停止 / 廃番 / 取扱終了` でよいか。実運用で使い分ける区分を確認したい |
| **4** | **在庫行が無い商品408件(§4.3)** | 「在庫0でも行を作る」のか「在庫がある商品だけ行を作る」のか。**現在の運用意図が未確認** |
| **5** | **§2.5 の重複3商品の統合** | どのIDへ寄せるか。統合すると過去の実績・判断の紐付けが動くため、人の判断が必要(`DEC-MST-01`) |
| ~~**6**~~ | ~~追加項目の優先順位~~ | ✅ **解決**(2026-09-06)— **重量・梱包サイズ・梱包資材の3種を先行追加**。物理属性として恒久保持。配送会社・配送方法は持たせない(§3.5) |
| **7** | **標準原価 未設定26件** | 未設定のまま許容するか、必須とするか。必須にすると新規登録が止まる可能性がある |
| **8** | **P000334 が28出品(楽天24)** | 1商品に楽天24出品は多い。**正常な運用か、重複登録かの確認が必要** |
| ~~**9**~~ | ~~発送テンプレートの実測値~~ | ✅ **消滅**(2026-09-07)— **梱包資材を管理対象から外し、マスターも作らないため実測が不要になった**(§4.5)。**設計上の最大のブロッカーが、測るのではなく削ることで解けた** |
| ~~**15**~~ | ~~元箱そのまま発送~~ | ✅ **解消**(2026-09-07)— サイズ中心へ変更したことで、**元箱で送る商品も発送サイズ区分に収まる**。資材が何であるかは判定に影響しない |
| **16** | **使用停止テンプレートを参照している商品の扱い(§4.5)** | 使用停止にした時点で、それを参照する商品は新規発送ができなくなる。**警告を出して人が付け替える**運用を想定しているが、確認したい |
| **17** | **代表寸法で足りるか(§4.5)** | 同一サイズ区分でも実寸は異なる(袋 35×25×3 と 箱 20×20×15 はどちらも60サイズ)。**ラベル記載や厚さ制限の判定で実寸が必要な場面があるか** |
| ~~**18**~~ | ~~商品重量は必要か~~ | ✅ **解決**(2026-09-07)— **必須**。送料計算だけでなく **Ground Truth としてAmazonカタログ等の誤登録を検知する監査項目**である(§3.10)。**判定に使わないからこそ、独立した検証基準として価値がある**。追加は2列で確定 |
| **19** | **監査の許容差(§3.10)** | 代表寸法・実測重量には誤差がある。**何g・何cm ずれたら警告するか**を決めないと、警告が多すぎて誰も見なくなる。品目や金額影響で閾値を変えるかも含めて確認したい |
| **20** | **監査対象に含める項目(§3.10)** | 重量・寸法は確定。**JAN・商品名・入数も比較対象にするか**。相乗りカタログでは商品名や入数の相違も起きうる |
| ~~**21**~~ | ~~比較元データの取得方法~~ | ⏸ **保留 — 設計課題ではなく実装課題**(2026-09-07)。**設計は取得手段に依存しない**(§3.9 比較元の抽象化)。契約(識別子・値・取得日時)を満たせばAPI・CSV・レポート・手入力のいずれでもよい。**手入力でも監査は成立するため、自動化を待たずに始められる**。取得方法は後工程で決定する |
| **10** | **重量・寸法は実測値か公称値か(§3.5 / §3.9)** | 実測なら測定の手間、公称ならメーカー値の信頼性という差がある。**⚠️ 2026-09-07 追記: 監査基準は監査対象と同じ出所であってはならない。** 公称値を入れるとAmazonカタログも同じ値を参照している場合に**誤りを検知できなくなる**。独立性の観点では**実測が望ましい** |
| **11** | **962件への入力計画(§3.5)** | 現在**全件未整備**。テンプレート化しても**商品重量962件は個別入力が必要**(§4.5)。全商品を一度に測るのは現実的でないため、**出荷頻度の高い商品から段階的に**埋める案を想定している |
| ~~**12**~~ | ~~旧VBA の扱い~~ | ✅ **解決**(2026-09-07)— **凍結済み**。警告ヘッダー付与 + `.bas.frozen_20260907_列不整合` へ改名しインポート不可にした。削除はしない。**危険の実体は「モジュール名の衝突」と「L列(商品重量)の誤読」だった**(§3.7 で訂正) |
| ~~**13**~~ | ~~重量の分解方式~~ | ✅ **解決**(2026-09-06)— 商品マスターは**商品そのものの重量**を持つ。**発送重量は保持せず導出する**。これにより商品マスターは配送会社に依存しない(§3.5) |
| ~~**14**~~ | ~~寸法の上書き~~ | ✅ **解決**(2026-09-06)— **上書きは採用しない。テンプレートは完全継承。** 例外商品は新テンプレートを追加する。Always Simple を優先(§4.5)。**商品マスターの追加は2列で確定** |

---

## 6.1 残り15件の分類と着手順(2026-09-07)

**判断基準は「設計の完成度」ではなく「MomijiStore OS を最短で完成させること」。**

### ⚠️ 先に押さえるべき2つの事実

**事実1: 残り15件のうち、配送ラベル自動作成の経路上にあるのは4件だけである。**
残り11件を全部片付けても、**OSの完成は1日も早くならない。**

**事実2: 配送マスターがまだ設計されていない。**
これは本書のOQには入っていない。**本書は商品マスターの設計書であって、OSの完成条件表ではない。**
15件を全部潰しても、**配送マスターが無ければラベルは作れない。**

### ① 実装をブロックしているもの(4件)— ここだけが経路上

| # | 事項 | 実際の作業量 |
|---|------|------------|
| ~~**9**~~ | ~~発送テンプレートの実測値~~ | ✅ **不要になった**(2026-09-07 — 梱包資材を管理対象から外したため) |
| **10** | 重量・寸法は実測値か公称値か | **判断1つ**(論拠は §3.10 で出揃っている) |
| **11** | 962件への入力計画 | 9・10 が決まれば決まる |
| **12** | 旧VBAの扱い(現行マスターへ実行すると追加列を破壊する) | **凍結するだけ。数分** |

> ✅ **2026-09-07: このブロッカーは消えた。**
> 梱包資材を管理対象から外した結果、**実測そのものが不要になった。**
> **測るべき数字が減ったのではなく、測る必要が無くなった。**

### ② 実データが無いと決められないもの(4件)— 運用開始後

| # | 事項 | いつ決まるか |
|---|------|------------|
| **19** | 監査の許容差(何gずれたら警告するか) | **実測値が入って乖離の分布が見えてから。** 今決めても根拠がない |
| **17** | 代表寸法で足りるか | 実際にラベルを作って不足が出てから |
| **16** | 使用停止テンプレートを参照する商品の扱い | **まだ使用停止が1件も発生していない** |
| **3** | 販売ステータスの値(廃番 / 取扱終了 の使い分け) | **962件すべて「販売中」。** 最初の販売停止が出てから |

> **今決めると、根拠のない値を「決定」として固定してしまう。** 待つことが正しい判断である。

### ③ 設計だけで確定できるもの(4件)— いつでも決められるが急がない

| # | 事項 | 経路上か |
|---|------|---------|
| **1** | 「販売チャネル」列の廃止 | ❌ 配送に無関係 |
| **4** | 在庫行が無い商品408件の扱い | ❌ 同上 |
| **7** | 標準原価 未設定26件 | ❌ 同上 |
| **20** | 監査対象に含める項目 | ❌ 監査の実装は配送より後 |

**判断は早いが、やっても完成は近づかない。** ①が片付いてから、まとめて処理する。

### ④ 将来改善項目(3件)— v2.0 以降

| # | 事項 | 理由 |
|---|------|------|
| **2** | 商品種別の点検962件 | 名寄せの自動化(§2.6)を実装してから効いてくる。**v1.0では人が登録する** |
| **5** | 重複3商品の統合 | **3件のみ。** Phase4のDB移行前に処理すれば足りる |
| **8** | P000334 が28出品 | 1件の調査。**実害が出ていない** |

### 🎯 今日やるべき順番

| 順 | やること | 誰が | 所要 |
|----|---------|------|------|
| ~~**1**~~ | ~~旧VBAの凍結(OQ 12)~~ | ✅ **完了**(2026-09-07) | — |
| **2** | **実測か公称かを決める**(OQ 10) | 判断1つ | **即** |
| **3** | ✅ **配送判定フローの設計**(`Shipping_Design.md`)— **順序を入れ替えた。判定が先、テンプレートは後** | 設計 | **完了** |
| ~~**4**~~ | ~~発送テンプレートを作成~~ | ✅ **不要**(2026-09-07)— マスターを作らないため。**商品マスターの列追加に統合された** | — |
| **5** | 入力計画を決める(OQ 11) | 上記の後に自動的に決まる | 短い |
| **6** | **配送マスターの実装**(`Shipping_Design.md` OQ 1〜3 の確定後) | 実装 | — |

**1・2 は今すぐ着手できる。3 が実質的な律速である。**

### 962件の入力完了を待つ必要はない

**これが最短化の要点である。**

```
整備済みの商品   → AIがラベルを自動作成
未整備の商品     → 人へ回す(§3.5 安全規則)
```

**1件でも整備できれば、その商品からラベル自動化が効く。**
962件が揃うまで自動化を止める理由はどこにもない。
**出荷頻度の高い順に整備すれば、少ない件数で大きな削減効果が出る。**

## 7. 設計レビューのチェックリスト

レビュー時に、以下が満たされているかで判断してほしい。

- [ ] **①責務** — 商品マスターが持つもの/持たないものが、判断できる形で線引きされているか(§1)
- [ ] **②主キー** — 内部管理IDを主キーとする根拠が、実データで示されているか(§2)
- [ ] **③管理項目** — 追加する項目すべてに「これが無いと何ができないか」があるか(§3.5)
- [ ] **④関係** — 商品単位と出品単位の境界が明確か(§4.2)
- [ ] 既存の制約(列名不変・原価の正・BL-5)に反していないか
- [ ] 推測で書かれた箇所が無いか(すべて実測値または既存決定に紐付いているか)

---

**次のステップ:** 本書のレビュー → §6 の8項目を確定 → Decision Log へ記録 → 実装(Phase4 設計)へ

> ⚠️ **本書はまだ承認されていない。** Change Management に従い、
> **承認後に `Architecture.md` の Decision Log へ記録してから**実装に入る。
