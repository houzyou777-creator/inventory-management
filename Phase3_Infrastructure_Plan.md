# Phase3 Infrastructure Implementation Plan

> **This document follows FOUNDATION.md.**
> **If any conflict exists, FOUNDATION.md takes precedence.**

ステータス: **ドラフト v1.0 — 承認待ち(実装禁止)**
作成日: 2026-08-05
位置づけ: **Phase3「MomijiStore OS Infrastructure」STEP1 成果物**
階層: ARCHITECTURE層

---

## 0. 本計画の前提と原則

### Phase3の目的(再掲)

Phase2で定義した会社の仕組みを、**安全・単純・復元可能な**Infrastructureとして実装する。
**Infrastructureそのものを目的にしない。Businessを実現するために必要なものだけを作る。**

### 本計画を貫く原則

**Phase3で作るものは、すべて「消しても会社が止まらない」ものに限る。**

現在の正本(`商品マスター_単品_v1.0.xlsx`、在庫管理のxlsm、GitHub)には**一切触れない**。Phase3の成果物はすべて複製・受け皿・実行環境であり、失敗したら削除して作り直せる。これがFOUNDATION Design Principle 3「Infrastructureは何度でも作り直せる」の実装上の意味である。

### 確認済みの実機情報(推測を含まない)

| 項目 | 確定値 |
|------|--------|
| NAS | UGREEN DXP4800 GT |
| CPU | AMD Ryzen Embedded R2514(4コア / 8スレッド) |
| RAM | **8GB**(増設は必須条件としない。制約条件として設計する) |
| M.2 SSD | 約2TB ×2・**RAID1**・**Volume1 約1.89TB・ファイルシステム Btrfs**(実機確認済) |
| HDD | 約10.91TB **×1台**(実機確認済)・**冗長性なし** |
| SMB | SMB 3.1.1 で Mac / Finder / CLI / Claude Code から接続確認済 |
| NAS共有 | `MomijiStore` — **保存先はボリューム1(SSD RAID1)**(実機確認済) |
| Git | Mac作業環境 → GitHub正本 →(Phase3で)NAS Mirror |
| 永続マウント | Finderログイン項目方式 |

### 設計に効く2つの制約

**制約A — RAM 8GB。** OS自身の消費を除くと、コンテナに割ける量は限られる。**常駐コンテナは PostgreSQL と MCP Server の2つのみ**とし、他はジョブ実行型とする。実際に何MB使えるかは実測前のため数値を置かない(§13 OQ-6)。

**制約B — HDDは1台で冗長性がない。** SSDはRAID1で保護されているが、**HDDは単騎構成のため1台の故障でデータを失う**。したがって**HDDには「失っても再作成できるもの」だけを置く**。この判断が §1 のStorage Policyを決めている。

### Storage Policy(2026-08-05 確定)

**SSD RAID1(ボリューム1・Btrfs・約1.89TB)を、当面の業務データ正本領域とする。**

単体HDD(約10.91TB ×1)には、**以下を置いてはならない**:

- ❌ Business Dataの正本
- ❌ Knowledgeの正本
- ❌ **唯一の**Backup
- ❌ 復元不能なデータ

単体HDDの用途は**以下に限定する**:

- ✅ 二次コピー
- ✅ 再生成可能データ
- ✅ 一時アーカイブ
- ✅ **別の正本が存在する**バックアップコピー

**2台目HDDによるRAID1が完成するまで、この制限を維持する。** RAID1完成時にStorage Policyを再評価する(別途設計・承認)。
この方針は既存原則「HDDには失っても再作成できるものだけを置く」と同一の考え方を、**正本という観点で厳密化したもの**である。

---

## 1. NAS Storage / Volume方針

| 項目 | 内容 |
|------|------|
| **目的** | 保護レベルの異なる2つのVolumeを、**正本を置いてよいか否か**で明確に分ける |
| **必要性** | SSD(RAID1・冗長あり・Btrfs・1.89TB)とHDD(単騎・冗長なし・10.91TB)は**信頼性が根本的に異なる**。同じ扱いをすると、失ってはいけないデータを最も壊れやすい場所に置く事故が起きる |
| **保存場所** | **SSD ボリューム1(業務データ正本領域)**: 正本・Docker・PostgreSQL・Gitミラー・モール由来データ<br>**HDD(非冗長の補助領域)**: 二次コピー・再生成可能データ・一時アーカイブ・別の正本が存在するバックアップコピー **のみ** |
| **実行場所** | NAS(UGOS) |
| **依存関係** | なし(Phase3の起点) |
| **リスク** | ①**HDD単騎の故障でHDD上の全データを失う**(→ Storage Policyにより、失っても正本は残る構造にしてある) ②SSD ボリューム1(1.89TB・現在使用2.0GiB)は当面十分だが、将来のDB肥大で逼迫する可能性 |
| **復旧方法** | **HDD故障時**: HDD上のものはすべて二次コピーであるため、**正本(SSD・Macローカル・GitHub)から再作成できる。会社は止まらない** ／ **SSD故障時**: RAID1により1台までは継続。両方失った場合もDocker・DBはPhase3時点で空であり、Composeファイル(GitHub管理)から再構築できる |

**この方針の要点:** HDDは冗長性がないため、**HDDを失っても業務が続く構造**にしてある。バックアップをHDDに置くのは「Macローカル `_backup/` と GitHub にも存在する」ことが前提であり、**この前提が崩れたらHDDへのバックアップは唯一のBackupとなり、Storage Policy違反になる**。

**2台目HDDの導入はPhase3のブロッカーとしない。** 現時点のHDD ×1は**非冗長の補助領域**として扱う。RAID1化は2台目導入時に別途設計・承認する。

## 2. NAS共有フォルダ構成

| 項目 | 内容 |
|------|------|
| **目的** | Logical Design §8で導いたフォルダ構成を、2つのVolumeへ割り付ける |
| **必要性** | 業務データの置き場が定まらないと、取込(PRC-01/02/04)も バックアップ(BIZ-SYS-01)も自動化できない |
| **保存場所** | 下表のとおり |
| **実行場所** | NAS(UGOS 管理画面での共有フォルダ作成) |
| **依存関係** | §1(Storage Policy) |
| **リスク** | UGOSでは共有フォルダはVolume単位のため、**1つの共有で2つのVolumeにまたがれない**。既存 `MomijiStore` はボリューム1(SSD)上にあるため、**HDD側を使うには別の共有が必要**。共有名と作成可否はSTEP2詳細設計で確定する |
| **復旧方法** | フォルダは空の状態で作成する。誤りがあれば削除して作り直す(データが入る前なら無害)。既存の`MomijiStore`共有のデータには手を加えない |

**Storage Policy(§1)に従い、正本を含むものはすべてSSDへ配置する:**

| フォルダ | 配置 | 用途 | Storage Policy上の分類 |
|----------|------|------|----------------------|
| `10_Git/` | **SSD**(`MomijiStore`共有) | GitHubのbareミラー | 二次コピー(正本はGitHub)だが高頻度I/Oのため SSD |
| `30_Data/` | **SSD** | モール由来CSV・帳票の保管 | **正本を含む** — Logical Design §3 で「販売実績・広告実績の正本は各モールの元CSV」と定義。モールの保存期間を過ぎると再取得できない |
| `90_System/` | **SSD** | 復旧手順・NAS設定メモ・**ログ** | ログは**復元不能**なため SSD |
| `20_Backup/` | **HDD** | Mac資産のバックアップ・DBダンプ・世代管理 | 二次コピー ※**条件付き**(下記) |
| `40_Knowledge/` | **HDD** | 知識・分析結果・FAQ の複製 | 二次コピー(正本はGit管理のMarkdown) |
| `50_AIModels/` | **HDD** | AIモデル(Phase3では未使用・枠のみ) | 再生成可能(再ダウンロード可) |

**⚠️ `20_Backup/` をHDDに置く条件:** Macローカル `_backup/` と GitHub のバックアップが**継続して存在すること**。これが崩れるとNASのバックアップが「唯一のBackup」となり、Storage Policy違反になる。**Macローカルのバックアップ運用を止めてはならない。**

※ 命名は `Architecture.md` Naming Convention(番号プレフィックス2桁・10刻み)に準拠。

## 3. 権限設計

| 項目 | 内容 |
|------|------|
| **目的** | 「人が使う権限」と「自動化が使う権限」を分け、自動化の誤書き込みを構造的に防ぐ |
| **必要性** | Business Catalogの**37業務中19業務(51%)が人の承認を必要とする**。承認を経ない書き込み経路が存在してはならない(BL-6) |
| **保存場所** | NASのユーザー/権限設定(UGOS管理画面) |
| **実行場所** | NAS(UGOS)。**設定操作はユーザー自身が行う**(Claudeは認証情報を扱わない) |
| **依存関係** | §2(共有フォルダの作成) |
| **リスク** | ①現在は`momiji-admin`単一ユーザーのみ確認できており、**自動化が管理者権限で動くと誤操作を止められない** ②権限を細かくしすぎると引き継ぎ時の説明が増える(Always Simpleに反する) |
| **復旧方法** | 権限設定は元に戻せる。作業前に現在の設定をスクリーンショット等で記録してから変更する |

**Always Simpleに基づく最小構成案(3ロール):**

| ロール | 権限 | 用途 |
|--------|------|------|
| 管理者(人) | 全共有に読み書き | 日常運用・設定変更 |
| 自動化(automation) | `20_Backup/` `30_Data/` `90_System/` に読み書き、`10_Git/`に読み書き、その他は読み取り | バックアップ・取込・ミラー |
| 参照(readonly) | 全共有に読み取りのみ | 将来の閲覧用途 |

※ ユーザーを追加できるか・UGOSの権限モデルの詳細は未確認(§13 OQ-4)。

## 4. GitHub → NAS Mirror方式

| 項目 | 内容 |
|------|------|
| **目的** | GitHub障害・アカウント喪失に備え、会社の全履歴の完全複製をNASに持つ(**Disaster Recovery用Mirror**) |
| **必要性** | FOUNDATION §5 BCP・`Architecture.md`のGit方針「Mac → GitHub(正本)→ NAS Mirror」の実装 |
| **保存場所** | NAS SSD Volume `10_Git/inventory-management.git`(bare) |
| **実行場所** | **Macローカル**(下記の理由) |
| **依存関係** | §2(`10_Git/`の作成)、§11(マウント検証) |
| **リスク** | **SMBマウント越しのgit操作は禁止**(既存決定・破損リスク)。NAS上で直接git操作するにはDocker(§5)が必要だが、それはSTEP4であり本項(STEP3)より後 |
| **復旧方法** | ミラーが壊れても**正本はGitHubにあるため実害なし**。削除して作り直す |

**💡 順序の矛盾を解く方式(推奨):**

```
Macローカルに --mirror クローンを作る(gitはローカルディスク上で動作)
        ↓ rsync(単純ファイルコピー・git操作ではない)
NAS 10_Git/inventory-management.git
```

この方式なら**SMB越しのgit操作を一切行わずにミラーを実現できる**。Docker(STEP4)を待つ必要もなく、STEP3で完結する。定期更新はMacローカル側で `git remote update` してからrsyncする。

**⚠️ NAS側コピーの位置づけ(厳守):**

**NAS上のミラーは Disaster Recovery 用であり、「直接作業するGitリポジトリ」として扱わない。**

- ❌ NASのミラーへ直接 push / commit / clone して作業する
- ❌ NASのミラーを開発の起点にする
- ✅ **GitHubが正本**。開発・履歴・PR・リリース管理はすべてGitHub基準
- ✅ NASミラーは「GitHubを失った時に取り出す保険」としてのみ使う

この区別が崩れると正本が二重化し、BL-5「正本は増やしてはならない」に違反する。

## 5. Docker Compose基盤

| 項目 | 内容 |
|------|------|
| **目的** | 会社のインフラを1つのYAMLで再現可能にする(NAS故障時も同じファイルから復元できる) |
| **必要性** | `Architecture.md` AI Infrastructure §5の中核。引き継ぎ資料そのものになる |
| **保存場所** | 定義ファイル `momiji-stack.yml` の**正本はGitHub**。NASへは配置(コピー)する |
| **実行場所** | NAS(UGOS Container Manager) |
| **依存関係** | §2(フォルダ)、§3(権限) |
| **リスク** | ①**RAM 8GBの制約**により常駐できるコンテナ数が限られる ②UGOSのアップデートでDocker環境が影響を受ける可能性 ③Container ManagerのCompose機能は文献調査では対応済だが**実機未確認**(§13 OQ-5) |
| **復旧方法** | スタックを停止・削除し、GitHubの`momiji-stack.yml`から再デプロイする。**Phase3時点でデータは空のため、削除しても失うものがない** |

**Phase3で定義するサービス(Always Simple — 必要なものだけ):**

| サービス | 常駐 | Phase3での到達点 |
|----------|------|-----------------|
| `momiji-postgres` | ◯ 常駐 | 起動確認まで(データは空) |
| `momiji-mcp` | ◯ 常駐 | 起動確認まで(接続はPhase5) |
| `momiji-python-worker` | ✕ ジョブ型 | 起動確認まで(§8参照) |

Redis・Ollama・Playwrightは**Phase3では定義しない**(Decision Log「必要になった時だけ追加する」)。

## 6. PostgreSQL配置方針

| 項目 | 内容 |
|------|------|
| **目的** | Phase4(Excel→DB移行)の受け皿を用意する |
| **必要性** | 現時点では業務で使わない。**Phase4で移行先が無いと困るため、先に器だけ用意する** |
| **保存場所** | データ: SSD Volume上のDockerボリューム `momiji_pg_data`。**ダンプ出力: HDD `20_Backup/db/`** |
| **実行場所** | NAS Docker |
| **依存関係** | §5(Compose基盤)、§1(SSD Volume) |
| **リスク** | ①RAM 8GBの共有。設定値を大きくすると他が動かなくなる ②**Phase4まで正本はExcelであり、DBに入れたデータを正本と誤認する危険**(BL-5違反) |
| **復旧方法** | コンテナとボリュームを削除して再作成。Phase3時点でデータは空。Phase4以降は `pg_dump` の世代から復元 |

**重要な運用ルール:** Phase3〜Phase4移行完了まで、**PostgreSQL上のデータは一切正本ではない**。この期間にDBを参照して業務判断してはならない。メモリ設定の具体値は実測前のため定めない(§13 OQ-6)。

## 7. MCP Server配置方針

| 項目 | 内容 |
|------|------|
| **目的** | 将来、AI・人・スクリプトがデータへアクセスする**唯一の入口**を用意する |
| **必要性** | `Architecture.md`「入口をひとつにする」の実装。入口が複数あると承認も記録も監査も成立しない |
| **保存場所** | 設定・コードの正本はGitHub。実行イメージはSSD Volume |
| **実行場所** | NAS Docker(常駐) |
| **依存関係** | §5(Compose基盤)、§6(PostgreSQL) |
| **リスク** | ①Phase3時点では接続先(DB)が空のため**動作確認の意味が限定的** ②認証情報の扱いを決めないまま起動すると、後で設計をやり直すことになる |
| **復旧方法** | コンテナを削除して再デプロイ(状態を持たないため無害) |

**Phase3での到達点は「起動できること」まで。** 実データへの接続と業務利用はPhase5(AI Agent)で行う。

## 8. Python Worker配置方針

| 項目 | 内容 |
|------|------|
| **目的** | 定型処理(取込・集計・同期)をNAS上で実行できる器を用意する |
| **必要性** | 月次業務(PRC-01/02/03)の自動実行に将来必要 |
| **保存場所** | スクリプトの正本はGitHub(`02_Analytics/Python/`)。実行時の入出力はHDD `30_Data/` |
| **実行場所** | NAS Docker(ジョブ型・常駐しない) |
| **依存関係** | §5(Compose基盤)、§2(`30_Data/`) |
| **リスク** | **下記の重大な制約を参照** |
| **復旧方法** | ジョブ型のため状態を持たない。コンテナを作り直すだけ |

**⚠️ 重大な制約 — 現行スクリプトはNASでは動かない:**

`02_Analytics/docs/月次KPI更新手順.md` に「**macOS + Microsoft Excel が必要**」と明記されている。`build_kpi_month.py` 等はExcelアプリケーションを操作して月次シートを生成しており、**Linuxコンテナでは実行できない**。

したがって **Phase3のPython Workerは器の用意にとどまり、移せる実処理は現時点で存在しない**。実処理の移行は、Excel依存が切れるPhase4(DB移行)以降になる。**この事実は「実行はMac、資産はNAS」という現行方針の技術的裏付けでもある。**

## 9. Backup / Snapshot方針

| 項目 | 内容 |
|------|------|
| **目的** | 「バックアップを取ること」ではなく「**事業を継続できること**」を担保する(BCP) |
| **必要性** | FOUNDATION Company Standards「取得ではなく復元を基準とする」 |
| **保存場所** | NAS HDD `20_Backup/`(世代管理)。**Macローカル `_backup/` の運用は必須として継続**(§2の条件 — これが無いとNASのバックアップが唯一のBackupとなりStorage Policy違反) |
| **実行場所** | **Mac**(理由: 永続マウントがFinderログイン項目方式であり、マウントはユーザーセッションに属するため) |
| **依存関係** | §2(`20_Backup/`)、§11(**マウント検証必須**) |
| **リスク** | ①**未マウント時にMac内蔵ディスクへ書き込み続ける事故**(§11で防ぐ) ②HDD単騎のためバックアップ先自体が壊れうる(→ Macローカルと GitHub が残るため会社は止まらない) |
| **復旧方法** | Macローカル `_backup/` とGitHubから復元。**年1回以上のリストア訓練で実測する**(FOUNDATION要件) |

**バックアップ対象(RULE 08 に準拠 — 実運用で定められている対象をそのまま採用):**

1. `在庫管理テーブル_v1.1.xlsm`
2. `商品マスター_単品_v1.0.xlsx`
3. `楽天在庫リスト_import.xlsx` / `Amazon在庫リスト_import.xlsx`
4. (追加)Gitリポジトリ全体・設計文書 — `20_Backup/` へ

**世代:** daily 7 / weekly 4 / monthly 12 / release 無期限(`PROJECT_CHARTER.md` §5 を踏襲)。
**古いバックアップの自動削除は禁止**(削除候補の提示のみ)。

**Snapshot:** ボリューム1が**Btrfs**であることを実機確認済のため、**Btrfs前提で設計してよい**。共有フォルダ単位のスケジュールSnapshot(daily 7 / weekly 4)を STEP7 で設計する。ただし **Snapshotはバックアップの代替にならない**(同一ディスク上にあるため、ディスク故障では両方失う)。Snapshotは誤操作からの時点復元、バックアップは機器故障への備えとして併用する。

## 10. Monitoring / Logging方針

| 項目 | 内容 |
|------|------|
| **目的** | 自動化が「動いたか / 失敗したか」を後から確認できるようにする |
| **必要性** | Success Metrics「バックアップ成功率」の計測に必要。またAI Constitution 第6条(記録する義務)の実装 |
| **保存場所** | HDD `90_System/logs/` |
| **実行場所** | 各ジョブの実行場所(Mac / NAS Docker)から書き込む |
| **依存関係** | §2(`90_System/`)、§11(マウント検証) |
| **リスク** | 監視を作り込みすぎると運用対象が増える(Always Simpleに反する) |
| **復旧方法** | ログは失っても業務に影響しない。削除して再開できる |

**Always Simpleに基づく最小構成:** 監視サービス(Prometheus等)は**導入しない**。ジョブごとに `ジョブ名_YYYYMMDD.log`(Naming Convention準拠)を書き、**成功/失敗の1行サマリーを残す**だけとする。異常検知は「バックアップログが今日の日付で存在しないこと」で足りる。

## 11. SMBマウント検証

| 項目 | 内容 |
|------|------|
| **目的** | **NASが繋がっていない状態で書き込む事故を、構造的に防ぐ** |
| **必要性** | SMBマウントが外れると `/Volumes/MomijiStore` は**Mac内蔵ディスク上のただの空フォルダになる**。この状態でrsyncが動くと内蔵ディスク(空き約100GB)を食い潰す。**Phase3で最も重要な安全装置** |
| **保存場所** | 目印ファイルをNAS共有直下に配置(例: `.momiji_nas_marker`) |
| **実行場所** | Mac(すべての書き込み処理の冒頭) |
| **依存関係** | §2(共有フォルダ)。§4・§9・§10の**すべての前提** |
| **リスク** | 検証を入れ忘れた処理が1つでもあると事故が起きる。**書き込み処理は例外なくこの検証を通す** |
| **復旧方法** | 事故が起きた場合はMac内蔵ディスクの当該フォルダを削除する(NASには何も書かれていない) |

**方式:** NAS共有直下に目印ファイルを置き、**書き込み前にその存在を確認する。無ければ処理を中止しエラーを記録する。** マウントされていなければ目印ファイルは存在しないため、確実に検知できる。

## 12. 障害時のRollback方法

| 項目 | 内容 |
|------|------|
| **目的** | 各STEPが失敗した時、**確実に元の状態へ戻せる**ようにする |
| **必要性** | FOUNDATION Principle 4「戻せるか。失敗を前提に設計する」 |
| **保存場所** | 復旧手順は `90_System/` に文書化(FOUNDATION要件) |
| **実行場所** | 該当STEPの実行場所 |
| **依存関係** | 全STEP |
| **リスク** | ロールバック手順を書かないまま実装を進めると、失敗時に判断できなくなる |
| **復旧方法** | 下表 |

| STEP | 作るもの | ロールバック方法 | 会社への影響 |
|------|---------|-----------------|-------------|
| STEP2 | 共有フォルダ | 空のフォルダを削除する | **なし**(データ投入前) |
| STEP3 | Gitミラー | NAS上のミラーを削除する。**正本はGitHub** | **なし** |
| STEP4 | Composeスタック | スタックを停止・削除する | **なし** |
| STEP5 | PostgreSQL | コンテナとボリュームを削除する。**Phase3時点でデータは空** | **なし** |
| STEP6 | MCP / Python Worker | コンテナを削除する(状態を持たない) | **なし** |
| STEP7 | Backup / Monitoring | ジョブ設定を削除する。Macローカル`_backup/`は従来どおり継続 | **なし** |

**すべてのSTEPで「会社への影響なし」となるのは偶然ではない。** §0の原則どおり、Phase3では既存の正本に触れない設計にしているためである。**もし影響ありのSTEPが現れたら、それは設計が誤っている合図とする。**

---

## 13. Open Questions(推測せず、確認が必要な事項)

| # | 事項 | 状態 | 内容 |
|---|------|------|------|
| OQ-1 | `MomijiStore`共有のVolume | ✅ **解決**(2026-08-05 実機確認) | **ボリューム1(SSD RAID1)上**。HDD側を使うには別共有の作成が必要 — STEP2詳細設計で確定 |
| OQ-2 | ファイルシステム | ✅ **解決**(2026-08-05 実機確認) | **ボリューム1 は Btrfs**・約1.89TB。**Snapshot設計はBtrfs前提で進めてよい** |
| OQ-3 | HDDのRAID1化 | ✅ **暫定決定**(2026-08-05) | **2台目導入はPhase3のブロッカーとしない。** 現HDD ×1は**非冗長の補助領域**として扱い、Storage Policy(§1)で正本を置かない。RAID1化は2台目導入時に別途設計・承認 |
| OQ-4 | NASのユーザー追加可否・権限モデルの粒度 | ⬜ **未解決** | §3の3ロール構成が実現できるか。UGOS管理画面(コントロールパネル→ユーザー)で確認 |
| OQ-5 | Container ManagerでDocker Composeが使えるか | ⬜ **未解決** | §5の前提。文献では対応済だが**実機未確認**。UGOS App Center / Container Manager で確認 |
| OQ-6 | PostgreSQL・MCPのメモリ配分 | ⬜ STEP5で実測 | RAM 8GBの配分。**実測前に数値を置かない。STEP2のブロッカーとしない** |
| OQ-7 | **在庫の正本の文書不整合** | ⬜ **調査完了・判断待ち** | 2026-08-05に読み取り専用調査を実施。結論は §15。**Logical Design の確定が誤っていた可能性が高く、訂正の承認が必要** |

**STEP2着手前に必要: OQ-4・OQ-5・OQ-7。** OQ-1〜OQ-3は解決済。OQ-6はSTEP5で実測する。

---

## 14. 実装順序と各STEPの完了条件

| STEP | 内容 | 完了条件 | 前提 |
|------|------|---------|------|
| **STEP1** | **本計画の作成** | 本書の承認 | — |
| **STEP2** | **Docker Project(Compose)の器** | ✅ **完了(2026-08-05)** — §16参照 | 承認 |
| STEP2b | Storage / Folder | 共有フォルダが作成され、Macからマウントして読み書きできる。**目印ファイルが配置済** | **OQ-4** |
| STEP3 | Git Mirror | NASのミラーがGitHubと同一のコミットを持つことを検証済 | STEP2b |
| STEP4 | Docker Compose Core | `momiji-stack.yml` がデプロイでき、スタックの起動・停止ができる | STEP3 |
| STEP5 | PostgreSQL | コンテナが起動し接続できる。**メモリ実測値を記録**(OQ-6の回答) | STEP4 |
| STEP6 | MCP / Python Worker | 両コンテナが起動する。Python Workerは**Excel非依存処理のみ**が対象 | STEP5 |
| STEP7 | Backup / Monitoring | バックアップが自動実行され、ログが残り、**復元テストに成功する** | STEP6 |

**各STEPは「設計 → 承認 → 小さく実装 → 検証 → 記録 → 次へ」の順で進める。** 一度に複数STEPを実装しない。各STEPの完了時にDecision Logへ記録する(Change Management)。

---

---

## 15. OQ-7 調査報告 — 在庫の正本に関する文書不整合

**2026-08-05 実施。読み取り専用。変更・リネーム・削除・復元は一切行っていない。**

### A. どの文書が何を正本としているか

| 文書 | 正本としているファイル |
|------|----------------------|
| `MomijiStore_OS_Logical_Design_v1.0.md` §3・§3.1 | `Excel/在庫管理システム_v1.0.xlsm` |
| `Architecture.md` Decision Log(2行)・未確定事項 | 同上(Logical Designを引用しているだけで独自の根拠はない) |
| **運用マニュアル 全7文書**(`01`〜`07`)・**運用ルールブック RULE 08** | **`SourceData/在庫管理テーブル_v1.1.xlsm`** |
| `Phase3_Infrastructure_Plan.md` §9 | RULE 08準拠 = `在庫管理テーブル_v1.1.xlsm` |
| `PROJECT_CHARTER.md` | **在庫の正本には言及していない**(バックアップ対象の一般記述のみ) |

### B. いつ決定されたか

| 決定 | 日時 | 根拠コミット |
|------|------|------------|
| マニュアル側(在庫管理テーブル v1.1) | **2026-07-11** | `46ef287`(19:36 運用マニュアル4種)・`c21440b`(19:52 運用ルールブック&フローチャート)。ルールブックの記載作成日も2026-07-11 |
| Logical Design側(在庫管理システム v1.0) | **2026-08-05 20:16** | `885df9a` |

**マニュアル側の決定が約1か月早い。Logical Designが後発である。**

### C. 後から変更された記録があるか

- **マニュアル側が「在庫管理システム_v1.0.xlsm へ戻す」と変更した記録は存在しない。**
- Logical Design(8/5)の決定は、**7/11のマニュアル一式を参照せずに**行われた。Decision Logにもマニュアルを確認した記録はない。
- 2026-08-05の運用マニュアル照合(`cdaacf1`)で不整合が発覚し、OQ-7として提起された。

### D. 現在どちらを正と解釈すべきか

**証拠は `SourceData/在庫管理テーブル_v1.1.xlsm` を指している。** 根拠は5点:

1. **更新の停止** — `在庫管理システム_v1.0.xlsm` は **2026-07-08(`3bd2cf5`)を最後に一度も更新されていない**。`Excel/` フォルダ全体でも7/8以降の変更はゼロ(8/5の削除コミットを除く)
2. **後継の登場(2026-07-11に一連の再設計)** — 08:00 商品マスターを2テーブル構成に再設計 → 08:14 在庫管理テーブル v1.0 新規作成(554件)→ 08:43 在庫運用機能(棚卸確定・異動ログ・ロールバック)→ 17:51 **v1.1完成版・T6〜T13テスト全PASS**
3. **運用文書の整備** — 同日 19:36 / 19:52 にマニュアル7文書が追加され、**すべてが在庫管理テーブル v1.1 を対象と明記**
4. **VBAの更新状況** — `Module_Inventory.bas`(在庫管理システム側)は **7/1** が最終。`Module_InventoryOps.bas`(在庫管理テーブル側・棚卸確定の実装)は **7/11**
5. **ファイル更新日** — v1.1 は 7/11 17:37、システム v1.0 は 7/8 11:34

**Logical Design §3.1が挙げた3根拠は、いずれも誤りまたは検証不足だった:**

| 当時の根拠 | 検証結果 |
|-----------|---------|
| 「現存する唯一の稼働ファイル」 | **誤り** — `在庫管理テーブル_v1.1.xlsm` も現存し、より新しい |
| 「VBAモジュール群が参照しているのは本ファイル」 | **不十分** — 実運用の中核である棚卸確定は `Module_InventoryOps` にあり、対象は在庫管理テーブル |
| 「在庫更新パイプラインが本ファイル上で完結」 | **検証していない推測だった** |

### 本番用xlsm削除への影響

**削除という結論自体は変わらない**(前世代のさらに古いコピーであり、内容はgit blob `cac3c98` に保存済み)。
ただし **Decision Logに記載した理由「正本が `在庫管理システム_v1.0.xlsm` に確定したため」は誤り**であり、訂正が必要。

### 💡 提案(実施していない — 承認待ち)

1. 在庫の正本を **`SourceData/在庫管理テーブル_v1.1.xlsm`** に訂正する
2. `MomijiStore_OS_Logical_Design_v1.0.md` §3・§3.1 を訂正する(誤りの経緯も残す)
3. `Architecture.md` Decision Log に**訂正エントリを追加**する(既存行は消さず、訂正として追記)
4. `在庫管理システム_v1.0.xlsm` の位置づけを決める — 前世代として保管か、アーカイブか(**削除・移動はしない**)

**推測で決めず、承認を待つ。** 上記1〜4はいずれも未実施。

---

---

## 16. STEP2 実施記録 — Docker Project(Compose)の器

**実施日:** 2026-08-05 / **状態:** ✅ 完了

### やったこと

`momiji-stack.yml` の器をリポジトリに作成した。**デプロイしていないため、コンテナ・イメージ・ネットワーク・ボリュームの実体は一切存在しない。**

| 成果物 | 内容 |
|--------|------|
| `MomijiStore_OS/05_Infrastructure/docker/momiji-stack.yml` | Compose定義の器。`name` / `networks` / `volumes` を確定。`services` は空(`{}`)。STEP5以降で有効化するPostgreSQL・MCP・Python Workerの雛形をコメントで用意 |
| `MomijiStore_OS/05_Infrastructure/docker/.env.example` | 環境変数テンプレート(値は空)。`MOMIJI_` プレフィックス |
| `.gitignore` | `.env` を明示的に除外し、`.env.example` は追跡する例外を追加 |

### 確定した既定値(Decision Log記録済)

| 項目 | 既定値 |
|------|--------|
| Project名 | `momiji-stack` |
| ネットワーク | `momiji_net`(bridge・**外部公開しない**) |
| ボリューム | `momiji_pg_data`(SSD ボリューム1 = 業務データ正本領域) |
| 配置場所 | `MomijiStore_OS/05_Infrastructure/docker/`(既存の番号プレフィックス方式に従い `05_` を新設) |
| 常駐サービス | PostgreSQL・MCP の2つのみ(RAM 8GB制約) |
| ポート公開 | **しない**。スタック内部からのみ接続する |
| 認証情報 | `.env`(Git管理外)。値の入力は利用者が行う |

### 検証結果

| 検証 | 結果 |
|------|------|
| `.env.example` がGitで追跡できる | ✅ |
| `.env` がGitで除外される(認証情報の漏洩防止) | ✅ `git add` が拒否されることを確認 |
| コンテナ・イメージ・ネットワーク・ボリュームの実体 | ✅ **未作成**(デプロイしていない) |

### ロールバック方法

`MomijiStore_OS/05_Infrastructure/` を削除し、`.gitignore` の3行を元に戻す。**ファイル追加のみのため、削除すれば完全に元の状態へ戻る。会社への影響なし。**

---

**次のステップ:** STEP5(PostgreSQL)— `momiji-stack.yml` のコメントを外し、パッチバージョンを固定してデプロイする。
※ STEP2b(共有フォルダ)・STEP3(Git Mirror)はOQ-4の確認後に実施。
